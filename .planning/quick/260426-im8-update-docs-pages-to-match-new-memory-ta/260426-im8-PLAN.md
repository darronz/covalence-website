---
phase: quick
plan: 260426-im8
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/docs/docs/mcp-tools.md
  - src/content/docs/docs/core-memories.md
  - src/content/docs/docs/getting-started.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "memory_search and memory_list parameter tables document valid source values (mcp, file, capture)"
    - "memory_list documents browse order (Core first, then MCP, then File memories)"
    - "Core Memories page explains the two-axis taxonomy (Category + Source) and that Core is a flag across both categories"
    - "Getting Started What's Next section mentions File memories from watched folders"
  artifacts:
    - path: "src/content/docs/docs/mcp-tools.md"
      provides: "source enum values and browse order documentation"
      contains: "mcp, file, capture"
    - path: "src/content/docs/docs/core-memories.md"
      provides: "Memory taxonomy explanation"
      contains: "File memories"
    - path: "src/content/docs/docs/getting-started.md"
      provides: "Updated What's Next with File memories mention"
      contains: "auto-indexes"
  key_links: []
---

<objective>
Update three /docs/ pages to reflect Covalence's memory taxonomy: the two-axis model (Category: Memory vs File; Source: mcp, file, capture), Core as a cross-category flag, and the browse order for memory_list.

Purpose: Docs currently describe `source` as a filter parameter but never list valid values, don't explain File memories, and don't document browse order. Users and AI clients need this information to use the API effectively.

Output: Updated mcp-tools.md, core-memories.md, getting-started.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/content/docs/docs/mcp-tools.md
@src/content/docs/docs/core-memories.md
@src/content/docs/docs/getting-started.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document source values and browse order in mcp-tools.md</name>
  <files>src/content/docs/docs/mcp-tools.md</files>
  <action>
Edit mcp-tools.md with these changes:

1. In the `memory_search` parameter table (line 36), change the `source` description from "Filter results by source type" to: "Filter by source type: `mcp`, `file`, or `capture`"

2. In the `memory_list` parameter table (line 53), change the `source` description from "Filter by source type" to: "Filter by source type: `mcp`, `file`, or `capture`"

3. After the `memory_list` **Returns** line (currently line 57), add a new paragraph:

```
When browsing without filters, results appear in intent order: Core memories first (pinned), then MCP memories (AI-stored), then File memories (auto-indexed from watched folders).
```

4. In the `memory_store` parameter table, the `source` is not a user-settable parameter (it is set automatically by the system), so do NOT add source to that table.

Match existing doc style: no trailing whitespace, `---` horizontal rules between tool sections, backtick-wrapped parameter names in descriptions.
  </action>
  <verify>
    <automated>grep -c "'mcp', 'file', or 'capture'" src/content/docs/docs/mcp-tools.md | grep -q "2" && grep -c "intent order" src/content/docs/docs/mcp-tools.md | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>memory_search and memory_list both list the three valid source values; memory_list has a browse order note after its Returns line</done>
</task>

<task type="auto">
  <name>Task 2: Add taxonomy model to core-memories.md</name>
  <files>src/content/docs/docs/core-memories.md</files>
  <action>
Edit core-memories.md to add a new section "## How Memories Are Organised" immediately AFTER the opening paragraph (line 5, before "## Creating Core Memories"). This section explains the taxonomy without changing any existing content.

Add this section:

```markdown
## How Memories Are Organised

Covalence organises memories along two independent axes:

**Category** determines how a memory was created:

- **Memory** -- an intentional entry stored via MCP or quick capture. You or your AI decided this was worth remembering.
- **File** -- auto-indexed content from a watched folder. Covalence reads the file and keeps a searchable copy. The file is the source of truth; edit the file and Covalence re-indexes it.

**Source** tracks where the memory came from: `mcp` (stored by an AI client), `file` (indexed from a watched folder), or `capture` (saved via the quick capture panel).

**Core** is a flag, not a category. Any memory -- whether it came from MCP, a file, or quick capture -- can be promoted to Core. Promoting a memory pins it to the top of the browse list and signals that your AI should always have it available.
```

Then update the existing "## Important: User-Controlled Only" section (last section) to add one sentence at the end, after "the user decides, always.":

```
This applies to both Memory and File categories -- the AI never auto-promotes a File memory to Core either.
```

Match existing style: no emoji, `---` only used as YAML frontmatter delimiter (not between sections in this page), em-dash as ` -- `.
  </action>
  <verify>
    <automated>grep -c "How Memories Are Organised" src/content/docs/docs/core-memories.md | grep -q "1" && grep -c "File.*auto-indexed" src/content/docs/docs/core-memories.md | grep -q "1" && grep -c "flag, not a category" src/content/docs/docs/core-memories.md | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>core-memories.md has a "How Memories Are Organised" section explaining the two-axis taxonomy and Core as a cross-category flag; the User-Controlled section notes the rule applies across both categories</done>
</task>

<task type="auto">
  <name>Task 3: Add File memories mention to getting-started.md What's Next</name>
  <files>src/content/docs/docs/getting-started.md</files>
  <action>
Edit the "## What's Next" section at the end of getting-started.md (lines 79-84). Add one new bullet point after the existing three bullets:

```markdown
- **[Under the Hood](/docs/under-the-hood/)** -- How Covalence finds the right memory, from embeddings to hybrid search
```

No standalone File memories page exists yet, so do NOT link to one. Instead, add a brief note after the "## Verify It Works" section (before "## What's Next") as a new section:

```markdown
## File Memories

Covalence can also auto-index files from watched folders on your Mac. File memories are searchable alongside your AI-stored memories but are read-only -- to update them, edit the original file and Covalence re-indexes it automatically.

Configure watched folders in the Covalence app under **Settings > Files**.
```

Match existing style: em-dash as ` -- `, bold for UI labels, no trailing whitespace.
  </action>
  <verify>
    <automated>grep -c "File Memories" src/content/docs/docs/getting-started.md | grep -q "1" && grep -c "auto-index" src/content/docs/docs/getting-started.md | grep -q "1" && grep -c "Under the Hood" src/content/docs/docs/getting-started.md | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>getting-started.md has a File Memories section explaining auto-indexing, and What's Next links to Under the Hood</done>
</task>

</tasks>

<verification>
After all three tasks, run:

```bash
npm run build 2>&1 | tail -20
```

Build must exit 0 with no errors. All three docs pages must appear in the build output.

Then verify key content landed:

```bash
grep -n "mcp.*file.*capture" src/content/docs/docs/mcp-tools.md
grep -n "How Memories Are Organised" src/content/docs/docs/core-memories.md
grep -n "File Memories" src/content/docs/docs/getting-started.md
grep -n "intent order" src/content/docs/docs/mcp-tools.md
```
</verification>

<success_criteria>
- mcp-tools.md: both source parameters list `mcp`, `file`, `capture` as valid values
- mcp-tools.md: memory_list has a browse order note (Core first, MCP, File)
- core-memories.md: new "How Memories Are Organised" section explains Category (Memory vs File), Source (mcp, file, capture), and Core as a flag
- getting-started.md: new "File Memories" section before What's Next
- getting-started.md: What's Next links to Under the Hood
- `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/quick/260426-im8-update-docs-pages-to-match-new-memory-ta/260426-im8-SUMMARY.md`
</output>
