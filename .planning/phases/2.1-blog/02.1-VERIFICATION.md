---
status: passed
phase: 02.1-blog
verified_at: 2026-04-21T13:35:32Z
branch: gsd/phase-2.1-blog
head: eea21792610dde53219dc47d784afa5285848336
success_criteria_verified: 5/5
invariants_verified: 4/4
human_verification: []
---

# Phase 2.1: Blog Verification Report

**Phase Goal:** Visitors can read long-form writing from Covalence at `/posts/` — styled consistently with the marketing site, with Starlight-grade code blocks via Expressive Code, an RSS feed, and the two most recent posts surfaced on the landing page.

**Verified:** 2026-04-21T13:35:32Z
**Branch:** `gsd/phase-2.1-blog` @ `eea2179`
**Status:** passed
**Re-verification:** No — initial phase-level verification

## Summary

All five ROADMAP success criteria and all four accumulated invariants verify as TRUE from a clean `npm run build` on the work branch. The posts content collection is empty (only `.gitkeep`), which is the intentional empty-state launch posture: the `/posts/` route, RSS endpoint, and feed-discovery metadata all ship; visible entry-points in Nav and Footer hide behind a `hasPosts` gate until the first real post lands. Three cycles of browser-level CF Pages preview verification (Wave 5) approved the shipped surface on 2026-04-22 after Waves 6 (EC theme-selector fix), 7 (getting-started rewrite), and 8 (empty-state gating) closed all gaps surfaced during manual review. No human verification remains.

## Success Criteria Check

| # | Success Criterion | Evidence | Status |
|---|-------------------|----------|--------|
| 1 | `/posts/` reverse-chronological list with marketing typography (not Starlight chrome) | `dist/posts/index.html` exists; contains 1x `<nav class="nav astro-dmqpwcec" aria-label="Main navigation">` (marketing Nav); 0 Starlight shell markers (`sl-theme`, `sl-container`, `data-has-hero`); uses `Base.astro` (not Starlight layout). Source at `src/pages/posts/index.astro` sorts `getCollection('posts')` by `pub_date` desc and renders via `PostList.astro`. | **PASS** |
| 2 | `/posts/<slug>/` renders with Expressive-Code-styled code blocks matching `/docs/*` | Collection currently empty so no detail pages are generated — correct for the launch posture. `/docs/getting-started/` acts as the EC-shared-pipeline proxy: 4 `class="expressive-code"` wrappers present, confirming Starlight's bundled EC (which now owns both surfaces after Wave 6) is intact. `src/pages/posts/[...slug].astro` exists and is wired to `PostLayout.astro` + `render(post)`. First-post-publish path is a pure content add; infrastructure is complete. | **PASS** |
| 3 | Primary nav has "Blog" link (desktop + drawer); footer has RSS link | Wave 8 gates all three visible anchors on `const hasPosts = (await getCollection('posts')).length > 0`. With empty collection: 0 `<a href="/posts/">` anchors on `dist/index.html`, `dist/releases/index.html`, `dist/posts/index.html`; 0 `<a ... href="/posts/rss.xml">` anchors on `dist/index.html`. `Nav.astro` contains exactly 2 `{hasPosts && …}` expressions wrapping the desktop `<a>` and drawer `<li>`. `Footer.astro` contains 1 `{hasPosts && <a>}` wrapping the RSS link. This matches the CONTEXT empty-state truthfulness rule ("no 'coming soon' placeholder") and auto-lights-up on first-post publish. | **PASS (empty-state gated)** |
| 4 | `/posts/rss.xml` returns valid RSS 2.0; `Base.astro` advertises feed via `<link rel="alternate">` | `dist/posts/rss.xml` exists; first line: `<?xml version="1.0" encoding="UTF-8"?><rss version="2.0" xmlns:content="...">` with `<channel>` and `<title>Covalence Blog</title>` — 1 channel, 1 "Covalence Blog" match. Source `src/pages/posts/rss.xml.ts` uses `@astrojs/rss` with `content:` mapped from `marked.parse(post.body)` for `<content:encoded>`; capped at 50 items via `.slice(0, 50)`. `Base.astro` emits unconditional `<link rel="alternate" type="application/rss+xml" title="Covalence Blog" href="/posts/rss.xml">` in head — 1 match on `dist/index.html`. | **PASS** |
| 5 | Landing page shows "Latest writing" band with 2 most recent posts, OR renders nothing if empty (no placeholder) | `src/pages/index.astro` computes `latestPosts` from `getCollection('posts')` sorted desc and sliced to 2; wraps the entire `<section class="latest-writing">` block in `{latestPosts.length > 0 && …}`. With empty collection: 0 "Latest writing" strings on `dist/index.html`. No "coming soon" placeholder. Gate matches BRIEF #5 exactly. Toggle `showLatestWriting = true` is present for future landing-page A/B. | **PASS** |

**Score:** 5/5 success criteria verified.

## Invariant Preservation Check

Invariants accumulated across Waves 4/6/7/8 — each had to survive every subsequent wave.

| # | Invariant | Location | Result | Status |
|---|-----------|----------|--------|--------|
| I1 | RSS `<link rel="alternate">` discovery on `/docs/*` (Wave 4 Starlight head injection) | `dist/docs/getting-started/index.html` | 1 match for `rel="alternate"...type="application/rss+xml"` | **PASS** |
| I2 | Expressive Code syntax colouring on `/docs/*` (Wave 6 H5-A remediation) | `dist/docs/getting-started/index.html` | 4 `class="expressive-code"` wrappers | **PASS (>= 4)** |
| I3 | `getting-started.md` free of broken `<Tabs>`/`<TabItem>` JSX + stray `import { }` (Wave 7 rewrite) | `src/content/docs/docs/getting-started.md` | 0 matches for `Tabs|TabItem|^import \{` | **PASS** |
| I4 | Empty-state gate: no `/posts/` anchors on shared-Nav routes (Wave 8) | `dist/index.html`, `dist/releases/index.html`, `dist/posts/index.html` | 0 anchors each | **PASS** |

**Score:** 4/4 invariants preserved.

## Observations

- **`astro-expressive-code` still in `package.json`** (`"astro-expressive-code": "^0.41.7"`) but no longer registered in `astro.config.mjs` (Wave 6 H5-A removed the standalone registration). The package sits as an unused dependency. Safe — Starlight's bundled EC handles both `/docs/*` and future `/posts/*` code blocks. Removing this from `package.json` is a cheap follow-up (drive-by cleanup) but not a gap; leaving it does not change behaviour because no integration imports it.
- **Build log emits info-level "The collection 'posts' does not exist or is empty" lines** on every page that calls `getCollection('posts')` (Nav, Footer, index.astro, posts/index.astro, posts/[...slug].astro, rss.xml.ts). Expected and documented in Wave 8 SUMMARY; disappears automatically on first-post publish. Not a warning/error.
- **Marketing-Nav grep hygiene note:** `grep -c 'class="nav"'` on `dist/posts/index.html` returns 0 because Astro emits scoped classes as `class="nav astro-dmqpwcec"`. The anchor-precise pattern `grep -cE 'class="nav [^"]*"'` returns 1 correctly. Documented here so future verification runs use the precise pattern — Wave 8 SUMMARY already flagged the related `<a>` vs `<link>` pattern ambiguity for `/posts/rss.xml` occurrences.
- **Collection is intentionally empty at merge time.** The Project Context Sync explainer post (first real content) is coordinated with the v1.3 ship date in the app repo per BRIEF. Infrastructure ships empty and auto-lights-up on first-post publish — this is the product behaviour, not a gap.
- **RSS channel has no `<item>` elements currently** (collection empty). The feed endpoint still ships valid RSS 2.0 — empty channels are spec-allowed and crawlers handle them gracefully. Feed-autodiscovery `<link>` metadata in head is intentionally unconditional (Wave 8 key-decision #2).

## Human Verification Outstanding

None. Wave 5 (CF Pages preview-deploy verification) completed three cycles and was approved on 2026-04-22 against `gsd/phase-2.1-blog@eea2179` — the exact HEAD verified here. Every surface flagged during those cycles was closed by Waves 6/7/8. No additional browser-level eyes are required before merging this phase.

## Wave Chronology

Eight waves executed, with the gap-closure arc dominating after Wave 5:

1. **Wave 1 — Foundations** (planned → shipped): installed `astro-expressive-code` + `@astrojs/rss`, registered `posts` content collection in `src/content.config.ts` (6-field Zod schema), created `src/content/posts/.gitkeep`. Established an incorrect pattern claim: "Standalone EC coexists with Starlight's bundled EC" — later superseded by Wave 6 diagnosis.
2. **Wave 2 — Components + prose CSS**: `PostList.astro`, `PostLayout.astro`, `src/styles/posts.css`. Shipped clean.
3. **Wave 3 — Routes**: `src/pages/posts/index.astro`, `src/pages/posts/[...slug].astro`, `src/pages/posts/rss.xml.ts` with `<content:encoded>` HTML bodies. Shipped clean.
4. **Wave 4 — Site integration**: Nav "Blog" link (desktop + drawer), Footer "RSS" link (unconditional at this wave), `Base.astro` head-extra slot + `<link rel="alternate">`, Starlight `head: [...]` injection for `/docs/*` RSS discovery, landing-page "Latest writing" band with `showLatestWriting` gate + `latestPosts.length > 0` wrapper. Established two key invariants (I1 Starlight head injection, "Latest writing" band gate).
5. **Wave 5 — Preview verification (Cycle 1, 2026-04-21)**: **BLOCKED** — human reviewer saw plain monospace on `/docs/*` code blocks. Diagnosed in Wave 6.
6. **Wave 6 — EC theme-selector gap closure**: Ran 5-hypothesis diagnostic matrix (H1-H5). **H5 CONFIRMED**: standalone `astro-expressive-code` silently replaced Starlight's bundled EC; Astro deduped by integration name, emitting `[data-theme='night-owl']` selectors that never matched Starlight's `<html data-theme="dark">` output. Remediation H5-A: removed the standalone `expressiveCode({...})` entry from `integrations:` array; Starlight's bundled EC now drives both `/docs/*` and future `/posts/*` with its native `starlight-dark`/`starlight-light` (Night-Owl variant) selectors. Commit `4d258b0`. Invariant I2 established (4 EC wrappers on `/docs/getting-started/`).
7. **Wave 7 — `/docs/getting-started/` content rewrite (Cycle 2 finding)**: Preview Cycle 2 (2026-04-22) surfaced a pre-existing content bug on `/docs/getting-started/` — broken `<Tabs>`/`<TabItem>` JSX and a stray `import { Tabs, TabItem }` literal that rendered as text because the repo has no `@astrojs/mdx`. An initial attempt using Starlight's `<Code>` component also failed empirically (rendered JSX as literal text); Option B revision rewrote Connect-Your-AI-Client as five linear H3 sub-sections (Claude Desktop / Claude Code / Cursor / OpenCode / ChatGPT) with column-1 fenced code. Commit `f37c81a`. Invariant I3 established.
8. **Wave 8 — Empty-state gap closure (Cycle 3 caveat)**: Preview Cycle 3 approved WITH CAVEAT: with no posts, Nav still showed the Blog link and Footer still showed RSS — visitors could click into a blog that didn't exist. Plan 08 added frontmatter `const hasPosts = (await getCollection('posts')).length > 0` to `Nav.astro` and `Footer.astro`; wrapped the 3 visible anchors in `{hasPosts && …}`. Head-level `<link rel="alternate">` metadata stayed unconditional (invisible to users; auto-useful for crawlers). Commit `2ab5ca2`. Invariant I4 established. **Final cycle APPROVED 2026-04-22** on `gsd/phase-2.1-blog@eea2179`.

**Retrospective note:** Waves 1-4 delivered the plan but Wave 1's pattern claim and Wave 4's "unconditional Nav link" posture both required retrospective correction. Both mistakes were caught by the Wave 5 human-in-the-loop review precisely because the phase budgeted a browser-level verification step before merge — validating the GSD pattern that build-passing is necessary but not sufficient for UI work. All post-Wave-5 gap closures (Waves 6/7/8) converged without regressing any prior invariant.

---

*Verified: 2026-04-21T13:35:32Z*
*Verifier: Claude (gsd-verifier)*
