---
phase: 02.1-blog
plan: 06
type: execute
wave: 6
depends_on:
  - 02.1-01
  - 02.1-02
  - 02.1-03
  - 02.1-04
files_modified:
  - astro.config.mjs                          # primary — always touched
  - src/components/StarlightHead.astro        # conditional (Option H2-B only)
  - src/starlight-plugins/rss-alternate.ts    # conditional (Option H2-A only)
  - package-lock.json                         # conditional (Option H4 only)
autonomous: true
gap_closure: true
requirements: []
tags:
  - blog
  - docs
  - expressive-code
  - starlight
  - regression-fix
  - gap-closure

must_haves:
  truths:
    - "`dist/docs/getting-started/index.html` contains `class=\"expressive-code\"` markup after `npm run build` — /docs/* code blocks render with Expressive Code chrome."
    - "`dist/docs/getting-started/index.html` contains `ec-line` markup — syntax-highlighting layer is present."
    - "`dist/docs/getting-started/index.html` contains exactly one `<link rel=\"alternate\" type=\"application/rss+xml\"` tag — RSS discoverability preserved (the Plan 04 Wave 4 decision survives)."
    - "`dist/posts/index.html` still renders with marketing chrome (Nav + Footer, no Starlight sidebar) — Plan 04 landing/integration behaviour preserved."
    - "`dist/posts/index.html` contains the `<link rel=\"alternate\" type=\"application/rss+xml\"` tag emitted via Base.astro — Plan 04 Base.astro edit still in effect."
    - "A human-readable diagnostic note is committed as part of this plan (in the fix commit message OR a short `.planning/phases/2.1-blog/02.1-06-DIAGNOSIS.md` file) naming which of H1-H5 the reproduction confirmed, with the grep evidence that pinned it."
    - "CF Pages preview deploy on branch `gsd/phase-2.1-blog` renders `/docs/getting-started/` with visible syntax colouring in a real browser (verified during the 02.1-05 checkpoint re-run after this plan ships — NOT inside this plan)."
  artifacts:
    - path: "astro.config.mjs"
      provides: "Surface E fix applied — the specific remediation depends on which hypothesis the diagnostic confirms"
      contains: "expressiveCode("
    - path: "astro.config.mjs"
      provides: "Starlight integration still registered with the RSS alternate-link head injection preserved"
      contains: "rel: 'alternate'"
  key_links:
    - from: "astro.config.mjs"
      to: "Starlight's internal Expressive Code pipeline"
      via: "integration registration order and/or option shape on `starlight({...})` — must not silently disable Starlight's bundled EC"
      pattern: "starlight\\("
    - from: "astro.config.mjs"
      to: "standalone astro-expressive-code integration"
      via: "preserved for /posts/* with night-owl themes"
      pattern: "expressiveCode\\(\\{\\s*themes:"
---

<objective>
Surface E regression (`/docs/*` code blocks render as plain monospace with no syntax colouring) blocks Phase 2.1 merge to `main`. This plan diagnoses which of the five hypotheses logged in `02.1-05-SUMMARY.md` is the root cause, applies the minimal fix, and verifies via `npm run build` that both `/docs/*` Expressive Code markup AND the Plan 04 RSS alternate link on `/docs/*` are present in the emitted HTML.

Purpose: Unblock the 02.1-05 preview-deploy checkpoint. Two Wave-4 constraints are non-negotiable and this plan must preserve them — (a) the standalone `astro-expressive-code` integration still owns `/posts/*` with the `night-owl` / `night-owl-light` themes that shipped in commit `45c30bb`; (b) the RSS alternate link still reaches `/docs/*` pages (Wave 4 commit `368d9a3`). A blanket revert of `368d9a3` would fix Expressive Code at the cost of the RSS discoverability requirement — that tradeoff is explicitly rejected.

Output: A single atomic `fix(02.1-06): ...` commit on branch `gsd/phase-2.1-blog` (NOT `main` — `main` stays unpushed until Plan 05 re-runs green), a `02.1-06-SUMMARY.md` summarising the diagnostic result and the remediation chosen, and a local `dist/` where both `/docs/getting-started/index.html` AND `/posts/index.html` satisfy the grep assertions in `<verification>`. The browser-level confirmation happens when 02.1-05 re-runs — that is NOT in scope for this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/2.1-blog/02.1-CONTEXT.md
@.planning/phases/2.1-blog/BRIEF.md
@.planning/phases/2.1-blog/02.1-PATTERNS.md
@.planning/phases/2.1-blog/02.1-05-SUMMARY.md
@.planning/phases/2.1-blog/02.1-01-SUMMARY.md
@.planning/phases/2.1-blog/02.1-04-SUMMARY.md
@CLAUDE.md
@astro.config.mjs
@package.json
</context>

<interfaces>
<!-- Current state of astro.config.mjs (post-Wave-4, HEAD of gsd/phase-2.1-blog). -->
<!-- This is the file the fix almost certainly touches; all other files stay untouched. -->

From astro.config.mjs (40 lines, post-Wave-4):
```javascript
import { defineConfig } from 'astro/config';
import starlight from '@astrojs/starlight';
import expressiveCode from 'astro-expressive-code';

export default defineConfig({
  site: 'https://covalence.app',
  output: 'static',
  build: {
    assets: '_assets',
  },
  integrations: [
    expressiveCode({ themes: ['night-owl', 'night-owl-light'] }),
    starlight({
      title: 'Covalence',
      description: 'Persistent memory for any AI client — local, private, zero configuration.',
      customCss: ['./src/styles/starlight.css'],
      head: [
        {
          tag: 'link',
          attrs: {
            rel: 'alternate',
            type: 'application/rss+xml',
            title: 'Covalence Blog',
            href: '/posts/rss.xml',
          },
        },
      ],
      social: [],
      sidebar: [
        { label: 'Getting Started', slug: 'docs/getting-started' },
        { label: 'Spaces', slug: 'docs/spaces' },
        { label: 'Core Memories', slug: 'docs/core-memories' },
        { label: 'MCP Tools', slug: 'docs/mcp-tools' },
        { label: 'AI Instruction', slug: 'docs/ai-instruction' },
        { label: 'Keyboard Shortcuts', slug: 'docs/keyboard-shortcuts' },
      ],
    }),
  ],
});
```

The five hypotheses from 02.1-05-SUMMARY.md (verbatim, condensed):
- **H1**: Integration-order regression only visible in CF Pages build (local build emits EC markup; CF does not).
- **H2**: Plan 04's `head: [...]` addition to Starlight (commit `368d9a3`) disabled Starlight's internal Expressive Code.
- **H3**: Theme-name mismatch cascading further than Plan 01 resolved — half-wired handover between Starlight's EC and the standalone.
- **H4**: Node / lockfile drift between local (.nvmrc `22.16.0`) and CF Pages build runner.
- **H5**: Starlight's own Expressive Code pipeline silently disabled whenever a standalone EC integration is registered at all — Plan 01 documented that the two coexist locally, but the claim may be wrong at CF build time OR the emitted markup may never have been styled by Starlight's CSS pipeline.

Relevant prior commits on `gsd/phase-2.1-blog`:
- `45c30bb` (Plan 01 fix) — swapped standalone EC themes from `starlight-dark`/`starlight-light` aliases to Shiki's `night-owl`/`night-owl-light` to prevent the standalone build crash.
- `368d9a3` (Plan 04 fix) — added `head: [{tag: 'link', attrs: {rel: 'alternate', ...}}]` to `starlight({...})` so `/docs/*` also carries the RSS alternate link.

Success criterion 2 in ROADMAP.md (Phase 2.1): "Visiting `https://covalence.app/posts/<slug>/` renders the post body with Expressive-Code-styled code blocks (syntax highlighting, copy button, frames) that visually match `/docs/*` code blocks." — this implicitly requires `/docs/*` code blocks to remain styled. That's the criterion Surface E pins to.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose Surface E — reproduce locally with CF-Pages-like settings and rule out H1-H5</name>
  <files>astro.config.mjs (temporary edits only, reverted before task ends; no net file change)</files>
  <read_first>
    - .planning/phases/2.1-blog/02.1-05-SUMMARY.md (sections "Regression Detail — Surface E" and "Divergence between local build and CF Pages preview" — the 5 hypotheses)
    - .planning/phases/2.1-blog/02.1-01-SUMMARY.md (confirmation that locally `dist/docs/getting-started/index.html` DID contain `class="expressive-code"` and `ec-line` markup after Wave 1)
    - .planning/phases/2.1-blog/02.1-04-SUMMARY.md (confirmation that locally AFTER Wave 4 commit `368d9a3` the same file still contained 32 `expressive-code|ec-line` occurrences — grep -oE 'expressive-code|ec-line' dist/docs/getting-started/index.html | wc -l)
    - astro.config.mjs (current 40-line state — the subject of all five hypotheses)
    - package.json (confirm expressive-code/astro-expressive-code versions shipped — lines 14-20)
    - .nvmrc (Node pin — confirm `22.16.0`)
  </read_first>
  <action>
Methodically run through the five hypotheses in dependency order (cheap → expensive) and record which one reproduces the failure. Only produce diagnostic artifacts in this task — NO net change to `astro.config.mjs` at the end (revert every experimental edit once you have the answer).

**Step 1 — Baseline reproduction (~30s).**

Confirm the branch you're on is `gsd/phase-2.1-blog`:

```bash
git rev-parse --abbrev-ref HEAD
```

Must print `gsd/phase-2.1-blog`. If it prints `main`, check out the branch (`git checkout gsd/phase-2.1-blog`) before continuing — this plan must never produce commits on `main`.

Reconcile with origin before doing anything else. Local `main` is 26 commits ahead of `origin/main` and `origin/gsd/phase-2.1-blog` carries pushed Wave 1-4 commits; if the local feature branch is behind the remote, any fix commit would stack on a stale base:

```bash
git fetch origin
if [ -n "$(git log gsd/phase-2.1-blog..origin/gsd/phase-2.1-blog --oneline 2>/dev/null)" ]; then
  echo "local gsd/phase-2.1-blog is behind origin — run 'git pull' to reconcile before proceeding"
  exit 1
fi
```

Confirm Node version:

```bash
node --version
```

Must match `v22.16.0` (or at minimum `22.x`, per `.nvmrc`).

Run a fresh CF-Pages-style build:

```bash
rm -rf dist/ node_modules/ package-lock.json.bak
cp package-lock.json package-lock.json.bak
npm ci
npm run build
```

(Note: `npm ci` mirrors Cloudflare Pages' behaviour — strict lockfile, fresh install. If `npm ci` refuses because `package-lock.json` disagrees with `package.json`, STOP and record this as strong evidence for H4 — that IS the CF-Pages divergence.)

Grep the output:

```bash
grep -oE 'class="expressive-code"' dist/docs/getting-started/index.html | wc -l
grep -oE 'ec-line' dist/docs/getting-started/index.html | wc -l
grep -oE 'expressive-code|ec-line' dist/docs/getting-started/index.html | wc -l
```

**Two branches from here:**

- **Branch A — local repro succeeded (counts all > 0).** This matches what 02.1-04-SUMMARY.md recorded: locally EC markup IS emitted; the regression is CF-Pages-specific. Skip to Step 3 (push a test commit and examine CF build logs).
- **Branch B — local repro FAILED (one or more counts == 0).** The regression is reproducible locally with `npm ci` + Node 22.16.0. This is the easier case — you can now test hypotheses directly. Continue to Step 2.

Record which branch you're on in your scratch notes (you'll reference it in the SUMMARY). If Branch A, note explicitly: "Local `npm ci` + `npm run build` emits EC markup; regression is CF-Pages-side. H4 suspected strongly; H1/H5 possible; H2/H3 unlikely to be CF-specific."

**Step 2 — Hypothesis matrix (Branch B only — reproducible locally).**

Run each mini-experiment below. After each one, `rm -rf dist/ && npm run build` and re-grep. Record the pre-experiment grep counts and the post-experiment grep counts for `/docs/getting-started/index.html` AND `/posts/index.html` (/posts/ should not regress while testing /docs/).

Use the Edit tool for each config change. After confirming OR ruling out each hypothesis, REVERT the change via Edit before moving to the next — experiments must not compound.

**H2 test (highest prior — most recent change is the most likely suspect).**
Comment out the entire `head: [ ... ],` block inside the `starlight({...})` options (lines ~17-27 of the post-Wave-4 file). Build. Re-grep `/docs/getting-started/index.html`.
- If Expressive Code markup returns → **H2 CONFIRMED.** The `head:` option is disturbing Starlight's internal EC plugin pipeline. Write this in the diagnostic notes and SKIP H5/H1/H3 — move to Task 2.
- If Expressive Code markup is still absent → **H2 RULED OUT.** Revert the commented block (restore the `head:` array exactly), move to H5.

**H5 test (second highest prior — matches "plain monospace = Starlight's EC CSS pipeline silent" symptom).**
Comment out the entire `expressiveCode({ themes: [...] }),` line (the first entry in the `integrations:` array). Build. Re-grep.
- If Expressive Code markup returns on `/docs/*` → **H5 CONFIRMED.** The standalone EC's mere presence in the integrations array silently disables Starlight's bundled pipeline. Critical: also grep `/posts/` to confirm you now get NO EC markup there (expected — you just removed the integration that owns /posts/*). Record both counts. Revert (re-enable the standalone integration). Move to Task 2.
- If Expressive Code markup on `/docs/*` stays absent → **H5 RULED OUT.** Revert, move to H1.

**H1 test (integration order).**
Swap integration order — put `starlight({...})` BEFORE `expressiveCode({ ... })` in the `integrations:` array. Build. Re-grep `/docs/*` AND `/posts/*`.
- If Expressive Code markup returns on `/docs/*` AND stays on `/posts/*` → **H1 CONFIRMED.** Ordering is the fix. Revert to alphabetical/original order for the diagnostic, then move to Task 2 with the plan to apply the reordering permanently.
- If nothing changes or `/posts/*` regresses → **H1 RULED OUT.** Revert to original order, move to H3.

**H3 test (theme-name / Starlight-EC type mismatch).**
Read the standalone integration's TypeScript types:

```bash
grep -rE "(themes|StylesProp|PluginCtx)" node_modules/astro-expressive-code/dist/*.d.ts | head -30
grep -rE "ExpressiveCode(Config|Options|Theme)" node_modules/astro-expressive-code/dist/*.d.ts | head -30
grep -rnE "starlight-dark|starlight-light|nightowl|NightOwl" node_modules/@astrojs/starlight/integrations/expressive-code/ | head -30
```

The goal is to confirm or deny: is Starlight's own EC pipeline wired to only register Starlight-specific themes when no user-level standalone is present? If Starlight's source explicitly checks `for (const i of userIntegrations) if (i.name === 'astro-expressive-code') return;` — H5 is the real answer and H3 is a misdiagnosis. Read enough source to answer that question definitively. Do NOT modify `node_modules/` — it's read-only observation.

- If Starlight's source shows it opts out when the standalone is present → record this finding; **H5 is likely the true root cause** even if the Step-2 H5 test was inconclusive. Move to Task 2 with an H5-shaped fix.
- If Starlight's source shows both EC integrations are supposed to coexist (no opt-out branch) → **H3 is the remaining candidate**, record evidence and move to Task 2 with the plan to investigate Starlight-EC options that control Starlight's pipeline directly.

**Step 3 — CF-Pages-only divergence check (Branch A only — local build is green).**

If Step 1 Branch A applied, the failure is CF-specific. Local experiments cannot catch it. Limited diagnostic options:

1. Push a tiny whitespace-only test commit to `gsd/phase-2.1-blog` and inspect the Cloudflare Pages build log on the dashboard. Look for:
   - Warnings from Starlight ("expressive-code integration detected, skipping internal setup" or similar) — confirms H5.
   - Different Node version than 22.16.0 — confirms H4.
   - Any integration-load error — confirms H1 or H2.
2. Check the preview deploy's `dist/docs/getting-started/index.html` via curl (you can request it from the *.pages.dev URL):
   ```bash
   curl -s https://<preview-hash>.covalence-website-v2.pages.dev/docs/getting-started/ \
     | grep -oE 'expressive-code|ec-line' | wc -l
   ```
   Compare with local. If CF emits NO markup but local does — H5 (subtle: different build environment triggers Starlight's opt-out).

You do NOT need to resolve the CF-specific failure in this task. Task 2 applies a fix that's defensible for all five hypotheses (safe-by-construction). Task 3 re-runs `npm run build` locally to confirm no local regression; the CF-side confirmation comes in the 02.1-05 checkpoint re-run.

**Step 4 — Record the diagnosis.**

Write a short scratch note (either as a `.planning/phases/2.1-blog/02.1-06-DIAGNOSIS.md` file OR as an extended fix-commit message — executor's choice) containing:

- Which branch (A or B) Step 1 landed on.
- For each of H1-H5: CONFIRMED / RULED OUT / UNTESTABLE (Branch A), with the grep evidence that pinned it.
- The final verdict — which hypothesis is the root cause (or "inconclusive; applying defensive fix shaped for both H2 and H5").

Do NOT commit the diagnostic file separately if you choose the commit-message route — keep it adjacent to the Task 2 fix commit.

**Guardrails — what this task must NOT do:**

- Do NOT keep any temporary edits to `astro.config.mjs` at the end of this task. The file must be byte-identical to its state at the start of the task (i.e. the post-Wave-4, post-368d9a3 state shown in the `<interfaces>` block). `git status` should report no modifications to `astro.config.mjs` when Task 1 ends.
- Do NOT install or remove any dependency. `package.json` and `package-lock.json` must be byte-identical.
- Do NOT push any commit to remote during diagnosis (the one exception in Step 3 requires approval — if you need to push a diagnostic commit to CF, flag it and stop; do NOT silently push).
- Do NOT touch `main` or `HEAD` — stay on `gsd/phase-2.1-blog` throughout.
- Do NOT modify any file outside `astro.config.mjs` (and even that is temporary-and-reverted).
  </action>
  <verify>
    <automated>git rev-parse --abbrev-ref HEAD | grep -q '^gsd/phase-2.1-blog$' && git diff --quiet astro.config.mjs package.json package-lock.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse --abbrev-ref HEAD` prints `gsd/phase-2.1-blog` (never ran on `main`).
    - `git log gsd/phase-2.1-blog..origin/gsd/phase-2.1-blog --oneline` returns empty output (local feature branch not behind origin — reconciled before any experiments ran).
    - `git diff --quiet astro.config.mjs` returns exit code 0 (no net modification to the config file from this task — all experiments reverted).
    - `git diff --quiet package.json package-lock.json` returns exit code 0 (no dep churn).
    - A diagnostic artefact exists (either `.planning/phases/2.1-blog/02.1-06-DIAGNOSIS.md` OR a prepared commit-message draft in the executor's notes) naming one of H1-H5 as CONFIRMED, or explicitly stating "inconclusive — defensive fix shaped for H2+H5".
    - For each of H1, H2, H3, H5 tested (H4 only if npm ci refused), grep counts from before and after each experiment on `dist/docs/getting-started/index.html` are recorded in the diagnostic notes.
    - At least one of the hypotheses has been CONFIRMED, or the "inconclusive — defensive fix" path has been explicitly selected with justification (e.g. "Branch A: local build green, CF-Pages-only failure, cannot test H1/H2/H3 directly").
  </acceptance_criteria>
  <done>Diagnostic complete; one hypothesis confirmed (or the "inconclusive — apply defensive fix" path chosen); `astro.config.mjs` and all other tracked files restored to their pre-task state; diagnostic notes ready to be embedded in the Task 2 commit message OR written to `02.1-06-DIAGNOSIS.md`.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the minimal fix to astro.config.mjs based on Task 1's verdict</name>
  <files>astro.config.mjs</files>
  <read_first>
    - Task 1's diagnostic notes (the CONFIRMED hypothesis or the "inconclusive — defensive fix" path).
    - astro.config.mjs (the current post-Wave-4 state — 40 lines, shown verbatim in the `<interfaces>` block above).
    - .planning/phases/2.1-blog/02.1-04-SUMMARY.md, section "Why Base.astro alone doesn't cover /docs/*" — explains why the RSS alternate link MUST reach /docs/* via Starlight's head option OR an equivalent path. This is the constraint you must preserve.
    - .planning/phases/2.1-blog/02.1-01-SUMMARY.md, "Decisions Made" section — night-owl theme names are locked on the standalone integration (reverting them would crash the standalone build).
    - CLAUDE.md (2-space indent, single-quote JS, double-quote HTML attrs — the config file uses single-quote JS).
  </read_first>
  <action>
Apply the smallest possible change to `astro.config.mjs` that:

1. Restores Expressive Code rendering on `/docs/*` (PRIMARY objective).
2. Preserves the RSS alternate link on `/docs/*` (Wave 4 commit `368d9a3`'s behaviour must survive — NO full revert of that commit).
3. Preserves the standalone integration owning `/posts/*` with `night-owl` / `night-owl-light` themes (Wave 1 commit `45c30bb`'s decision must survive).

The specific edit depends on Task 1's verdict. Apply exactly one of the remediations below:

**If Task 1 confirmed H2 (the `head:` array breaks Starlight's bundled EC):**

Remove the `head: [...]` array from the `starlight({...})` options block AND replace it with an equivalent Starlight-sanctioned mechanism that does not pass through the `head` option. Two candidates — try in this order, stop at the first one that works with `npm run build`:

- **Option H2-A (preferred):** Express the RSS alternate link via a Starlight plugin. Starlight exposes a `plugins:` option on its integration config; a minimal plugin can call `injectRoutes` / `injectScript` OR hook into the page's head via the `setup` callback's context. Read `node_modules/@astrojs/starlight/utils/plugins.ts` (or whatever path the version exposes) to confirm the API. If this path is clean, implement a one-file inline plugin at `src/starlight-plugins/rss-alternate.ts` and register it via `plugins: [...]`.
- **Option H2-B (fallback):** Drop the `head:` option entirely and mirror the RSS alternate link into every Starlight page via a different route. Concrete candidates:
  - A `customCss` entry is the wrong lever (CSS doesn't emit head tags).
  - A custom Starlight Component override for `<PageFrame>` or `<Head>` — Starlight v0.38 supports component overrides via `components: { Head: './src/components/StarlightHead.astro' }`. Create a thin `src/components/StarlightHead.astro` that renders Starlight's default head PLUS the alternate link.

If Option H2-A works, prefer it (smaller surface). If Option H2-B is cleaner, that's acceptable. In either case the final `astro.config.mjs` must satisfy both the EC-on-docs AND alternate-rel-on-docs grep assertions in Task 3.

**If Task 1 confirmed H5 (standalone EC silently disables Starlight's bundled EC):**

Two sub-remediations possible; pick based on the scope of Task 1's findings:

- **Option H5-A (preferred — least invasive):** Remove the standalone `expressiveCode({ themes: [...] })` integration entry from the `integrations:` array entirely, and rely on Starlight's bundled EC for BOTH `/docs/*` AND `/posts/*`. Caveat: Starlight's EC wrapper's default config only processes Starlight content; for `/posts/*` code blocks to pick up EC, you need to either (a) let Starlight's EC pre-process the whole project's markdown (check via a test `.md` in `/src/content/posts/` with a `\`\`\`js` block — if it renders with EC chrome, done) or (b) re-introduce the standalone with a `contentDir`/`ignore` option that prevents it from interfering with Starlight's pipeline.
- **Option H5-B (if Starlight's bundled EC does NOT process non-docs markdown):** Keep the standalone `expressiveCode({ themes: [...] })` but find the option that makes it coexist silently. Read `node_modules/astro-expressive-code/dist/*.d.ts` and look for a `customizeTheme`, `shiki`, or ordering-related option that prevents it from shadowing Starlight. If no such option exists, the fix becomes "re-order integrations" (Starlight BEFORE standalone) and verify both `/docs/*` and `/posts/*` emit EC markup post-build.

**If Task 1 confirmed H1 (integration order):**

Simply swap the order — `starlight({...})` first, then `expressiveCode({ themes: [...] })`. That's a one-character-range diff (swap two array entries). Verify both /docs/* and /posts/* still emit EC markup.

**If Task 1 confirmed H3 (theme-name cascading mismatch):**

No single-line fix — this would indicate the Plan 01 theme rename only half-solved the coexistence. In this case defer to H2-A or H5-A as the defensive fix (removing the interference surface) rather than chasing a third theme configuration.

**If Task 1 confirmed H4 (Node/lockfile drift):**

Verify `.nvmrc` contents (`22.16.0`) and that `package.json` `engines.node` is compatible (`">=22.12.0"` per current state is fine). If there's a true lockfile mismatch, regenerate the lockfile (`rm package-lock.json && npm install`) on Node 22.16.0 — but ONLY if `npm ci` failed in Task 1 Step 1. Do NOT regenerate the lockfile speculatively. Add `files_modified: [package-lock.json]` to this plan's effective-diff if lockfile regeneration happens.

**If Task 1 was inconclusive (Branch A — local build green):**

Apply the DEFENSIVE fix: assume H2 OR H5 is the root cause and preemptively remove the most likely interference point. In order of decreasing safety:

- Defensive Fix 1: Apply H2-A (replace Starlight's `head:` option with a plugin or component override). Rationale: if H5 is the real cause, this change does NOT make things worse; if H2 is the real cause, this change IS the fix.
- Defensive Fix 2: If Defensive Fix 1 does not restore EC markup locally, compound with H5-A (remove standalone integration, verify Starlight's bundled EC handles /posts/*). This is more invasive but covers the H5 case.

**Universal constraints regardless of which remediation you apply:**

- The edit is ONLY in `astro.config.mjs` (and optionally `package-lock.json` if H4).
- If a new file is introduced as a side-effect of the remediation (e.g. a Starlight component override for Option H2-B), add it to `files_modified` in the fix-commit message AND to this plan's effective diff. ONLY paths permitted for new files: `src/components/StarlightHead.astro` (for Option H2-B) or `src/starlight-plugins/rss-alternate.ts` (for Option H2-A). Anything else requires stopping and re-scoping.
- NO new runtime dependencies.
- NO changes to Node version, `.nvmrc`, or `package.json` `engines` block.
- The final `astro.config.mjs` MUST keep the standalone `expressiveCode({ themes: ['night-owl', 'night-owl-light'] })` call IF the remediation retains the standalone (Options H1, H2-A, H2-B, H5-B). IF the remediation removes it (Option H5-A), document this explicitly in the commit message as a Wave 1 decision reversal.
- Commit the change atomically with a descriptive message:

```
fix(02.1-06): restore /docs/* Expressive Code rendering ([hypothesis-name])

<one-paragraph explanation of which hypothesis was confirmed and why this
remediation addresses it>

<brief note on what was considered and rejected (alternative remediations)>

Preserves:
- RSS alternate link on /docs/* (Wave 4 commit 368d9a3)
- Standalone EC on /posts/* with night-owl themes (Wave 1 commit 45c30bb)

Local verification:
- grep -oE 'class="expressive-code"' dist/docs/getting-started/index.html | wc -l = <count>
- grep -oE 'ec-line' dist/docs/getting-started/index.html | wc -l = <count>
- grep -oE 'rel="alternate" type="application/rss\+xml"' dist/docs/getting-started/index.html | wc -l = 1
- grep -oE 'rel="alternate" type="application/rss\+xml"' dist/posts/index.html | wc -l = 1

Full diagnostic matrix for H1-H5 in .planning/phases/2.1-blog/02.1-06-DIAGNOSIS.md
(OR inline below if no separate file was created).
```

The commit happens on `gsd/phase-2.1-blog` (NOT `main`). Do not push yet — Task 3 verifies locally first, then the execute-phase workflow handles the push.
  </action>
  <verify>
    <automated>git rev-parse --abbrev-ref HEAD | grep -q '^gsd/phase-2.1-blog$' && node -e "const c = require('fs').readFileSync('astro.config.mjs','utf8'); if (!c.includes('starlight(')) { console.error('starlight() missing'); process.exit(1); } if (!c.match(/rel:\s*['\"]alternate['\"]/) &amp;&amp; !c.includes('StarlightHead') &amp;&amp; !c.includes('starlight-plugins')) { console.error('RSS alternate link path not preserved — no head option AND no component override AND no plugin module'); process.exit(1); } console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff astro.config.mjs` shows a non-empty diff (the fix was applied).
    - `grep -c 'starlight(' astro.config.mjs` returns `1` (Starlight still registered).
    - At least ONE of these is true (RSS alternate-link path preserved):
      - `grep -cE "rel:\\s*['\"]alternate['\"]" astro.config.mjs` returns ≥ `1` (Starlight `head:` option retained or re-expressed inline).
      - A new file `src/components/StarlightHead.astro` exists AND `grep -q "Head:" astro.config.mjs` is true (component-override path).
      - A new file under `src/starlight-plugins/` exists AND `grep -q "plugins:" astro.config.mjs` is true (plugin-path).
    - If `src/components/StarlightHead.astro` exists (Option H2-B path was selected): `grep -q 'rel="alternate"' src/components/StarlightHead.astro` returns 0 — i.e. the override actually emits the RSS alternate link, not just exists as an empty shim.
    - If the fix retains the standalone EC integration: `grep -cE "expressiveCode\\(\\s*\\{\\s*themes:\\s*\\['night-owl'" astro.config.mjs` returns `1` (night-owl themes preserved).
    - If the fix removes the standalone EC integration: the fix-commit message EXPLICITLY notes "Wave 1 decision reversed — standalone integration removed, Starlight's bundled EC now handles /posts/* too" AND `src/content/posts/` can demonstrably render EC markup for a code fence when built (Task 3 will verify).
    - The fix is ONE atomic commit on `gsd/phase-2.1-blog`, commit-message pattern `fix(02.1-06): ...`.
    - The commit message OR a separate `02.1-06-DIAGNOSIS.md` file contains the diagnostic matrix from Task 1.
    - No unrelated files modified — `git diff --name-only HEAD~1 HEAD` returns ONLY `astro.config.mjs` (and optionally ONE new file in `src/components/` OR `src/starlight-plugins/`, OR `package-lock.json` if H4).
    - The commit is NOT on `main` — `git log main --oneline -1 | grep -v '02.1-06'` (the last main commit must predate this fix).
  </acceptance_criteria>
  <done>Atomic fix commit on `gsd/phase-2.1-blog`; `astro.config.mjs` mutated surgically; RSS alternate link path preserved; standalone EC preserved OR documented-as-removed; diagnostic evidence embedded in commit message or adjacent diagnosis file.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the fix — npm run build passes and both /docs/* and /posts/* satisfy grep assertions</name>
  <files>(no file modifications — this task runs the build and verifies output)</files>
  <read_first>
    - Task 2's fix commit (the exact change applied).
    - .planning/phases/2.1-blog/02.1-04-SUMMARY.md "Post-Build Verification Matrix" section — the reference grep counts from Wave 4 for `/docs/*` and `/posts/*`.
    - astro.config.mjs (the post-fix state).
    - CLAUDE.md (verification norms: "no tests — verification = npm run build + astro preview + CF Pages preview deploys").
  </read_first>
  <action>
Run the full build and execute the grep-verifiable assertions that pin Surface E back to green locally. Do NOT modify any tracked file in this task — it is observation-only, producing a summary report.

**Step 1 — Clean build.**

```bash
rm -rf dist/
npm run build
```

Build must exit 0. If it fails, STOP and flag via `## ISSUES FOUND` — the fix regressed the build, Task 2 needs to be reworked.

**Step 2 — /docs/* Expressive Code assertions (the primary regression target).**

```bash
grep -oE 'class="expressive-code"' dist/docs/getting-started/index.html | wc -l
grep -oE 'ec-line' dist/docs/getting-started/index.html | wc -l
grep -oE 'expressive-code|ec-line' dist/docs/getting-started/index.html | wc -l
```

Required:
- `class="expressive-code"` count ≥ `1`.
- `ec-line` count ≥ `1`.
- Combined count ≥ `10` (Wave 4 recorded 32 — anything ≥ 10 confirms the pipeline is back; anything < 10 suggests the fix is partial).

Do the same for at least one OTHER `/docs/*` page to confirm systemic coverage (not just one page):

```bash
for f in dist/docs/*/index.html; do
  echo "=== $f ==="
  grep -oE 'expressive-code|ec-line' "$f" | wc -l
done
```

Every file should report ≥ `1`. If any page reports `0`, flag it.

**Step 3 — /docs/* RSS alternate-link preservation assertion (Wave 4 invariant).**

```bash
grep -oE '<link rel="alternate" type="application/rss\+xml"[^>]*>' dist/docs/getting-started/index.html
grep -cE 'rel="alternate" type="application/rss\+xml"' dist/docs/getting-started/index.html
```

Required:
- At least one `<link rel="alternate" type="application/rss+xml"` match in the HTML.
- The count is `1` (exactly one — not zero, not two).

**Step 4 — /posts/* regression sentinels (Wave 4 invariants — make sure the fix did not break the blog side).**

```bash
test -f dist/posts/index.html && echo "posts index present"
grep -cE 'rel="alternate" type="application/rss\+xml"' dist/posts/index.html
grep -oE '<h1[^>]*>Blog</h1>' dist/posts/index.html || grep -oE '>Blog<' dist/posts/index.html
grep -oE 'class="nav-' dist/posts/index.html | head -5
```

Required:
- `dist/posts/index.html` exists.
- RSS alternate link count on the posts index is `1` (from Base.astro — that's the site-wide one).
- The page still carries marketing chrome (nav classes present; no Starlight sidebar div).
- H1 "Blog" still present (Astro auto-scoping may append a class — either regex matches).

**Step 5 — RSS feed and per-post EC (standalone side) sanity.**

Because the posts collection is empty, there's no per-post HTML to grep for EC markup on `/posts/*`. To confirm the standalone integration still functions (if it was retained in Task 2), inject a throwaway test post with a code fence, build, grep, then remove the test post. Or: simply confirm the `/posts/rss.xml` feed builds and is well-formed:

```bash
test -f dist/posts/rss.xml && echo "rss present"
python3 -c "import xml.etree.ElementTree as ET; ET.parse('dist/posts/rss.xml'); print('valid xml')"
grep -c 'https://covalence.app/' dist/posts/rss.xml
```

Required: rss.xml exists, parses as valid XML, contains at least one `covalence.app` reference (channel link).

If Task 2 chose Option H5-A (removed standalone integration), ALSO do the throwaway-post verification.

IMPORTANT — filename convention: the throwaway post MUST follow the Plan 01 posts-collection convention `YYYY-MM-DD-<kebab-slug>.md`. Leading-underscore filenames (`_diagnostic-test.md`) may be excluded by Astro's content-collection glob pattern by convention, which would cause the test to falsely "prove" H5-A is broken when in fact the fixture was never ingested. Use today's date (`2026-04-21`) and slug `diagnostic-test`:

```bash
cat > src/content/posts/2026-04-21-diagnostic-test.md <<'EOF'
---
title: Diagnostic Test
description: Throwaway post to verify /posts/* code-fence rendering. Delete before shipping.
pub_date: 2026-04-21
---

Test code fence below — must render with Expressive Code chrome:

```js
const x = 42;
```
EOF
rm -rf dist/ && npm run build
find dist/posts -name '2026-04-21-diagnostic-test' -o -name '*2026-04-21-diagnostic-test*' | head -3
# Expect one directory like dist/posts/2026-04-21-diagnostic-test/
grep -oE 'class="expressive-code"' dist/posts/*/index.html 2>/dev/null | wc -l
# Expect ≥ 1 if Starlight's bundled EC is now handling /posts/* too
rm src/content/posts/2026-04-21-diagnostic-test.md
# Residue check — working tree under src/content/posts/ must match the pre-fixture state.
git status --porcelain src/content/posts/ | grep -q . && { echo "diagnostic-test residue left behind in src/content/posts/"; exit 1; } || true
rm -rf dist/ && npm run build
```

If the throwaway test shows `0` EC markup on `/posts/*`, Option H5-A is WRONG — Starlight's bundled EC does NOT process non-docs markdown. Revert to the pre-fix state and re-enter Task 2 with Option H5-B instead. Remove the diagnostic file either way.

**Step 6 — Compile the verification summary.**

Write a short summary paragraph (for inclusion in the eventual `02.1-06-SUMMARY.md`) listing:

- The final grep counts from Steps 2-5.
- Whether any step required retry-with-a-different-option (e.g. H5-A → H5-B swap).
- Explicit statement: "Local build satisfies must-haves truths 1-5. Must-have truth 6 (diagnostic note committed) satisfied via [commit message OR 02.1-06-DIAGNOSIS.md]. Must-have truth 7 (browser confirmation on CF Pages) deferred to 02.1-05 checkpoint re-run."

**Step 7 — Push the branch.**

Once Steps 1-6 pass, push `gsd/phase-2.1-blog` so CF Pages builds a fresh preview deploy:

```bash
git push origin gsd/phase-2.1-blog
```

If the push is rejected (e.g. branch protection rules or a diverged remote), STOP and flag — do NOT force-push. The 02.1-05 checkpoint can re-run against the existing preview if the push cannot land.

Do NOT push `main`. Do NOT merge `gsd/phase-2.1-blog` into `main` — that only happens after 02.1-05 returns `approved`.
  </action>
  <verify>
    <automated>npm run build 2&gt;&amp;1 | tail -5 &amp;&amp; test -f dist/docs/getting-started/index.html &amp;&amp; [ "$(grep -oE 'class=\"expressive-code\"' dist/docs/getting-started/index.html | wc -l)" -ge "1" ] &amp;&amp; [ "$(grep -oE 'ec-line' dist/docs/getting-started/index.html | wc -l)" -ge "1" ] &amp;&amp; [ "$(grep -cE 'rel=\"alternate\" type=\"application/rss\+xml\"' dist/docs/getting-started/index.html)" = "1" ] &amp;&amp; [ "$(grep -cE 'rel=\"alternate\" type=\"application/rss\+xml\"' dist/posts/index.html)" = "1" ] &amp;&amp; echo VERIFY_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `grep -oE 'class="expressive-code"' dist/docs/getting-started/index.html | wc -l` returns ≥ `1`.
    - `grep -oE 'ec-line' dist/docs/getting-started/index.html | wc -l` returns ≥ `1`.
    - `grep -oE 'expressive-code|ec-line' dist/docs/getting-started/index.html | wc -l` returns ≥ `10` (sanity check against Wave 4's recorded `32`).
    - `grep -cE 'rel="alternate" type="application/rss\+xml"' dist/docs/getting-started/index.html` returns exactly `1` (Wave 4 invariant preserved).
    - `grep -cE 'rel="alternate" type="application/rss\+xml"' dist/posts/index.html` returns exactly `1` (Base.astro's site-wide emit still works).
    - `dist/posts/index.html` exists and contains the Blog H1 (either `<h1>Blog</h1>` or class-scoped equivalent).
    - `dist/posts/rss.xml` exists and `python3 -c "import xml.etree.ElementTree as ET; ET.parse('dist/posts/rss.xml')"` exits 0.
    - For every file matching `dist/docs/*/index.html`: EC markup count ≥ `1`.
    - If Option H5-A was applied in Task 2: the throwaway-post diagnostic (at `src/content/posts/2026-04-21-diagnostic-test.md` — matching Plan 01's `YYYY-MM-DD-<kebab-slug>.md` convention, NOT a leading-underscore filename that globs would drop) confirmed `/posts/*` still emits EC markup via Starlight's pipeline (or Task 2 was reworked to H5-B and re-verified), AND `git status --porcelain src/content/posts/` returns empty after cleanup (no diagnostic residue).
    - No files other than the Task 2 fix-commit files are modified — `git status --porcelain` is empty.
    - `gsd/phase-2.1-blog` has been pushed to origin (or push failure is flagged explicitly).
  </acceptance_criteria>
  <done>Local build green; all grep assertions met; branch pushed; ready for 02.1-05 re-run to confirm browser-level rendering on the CF Pages preview. 02.1-06-SUMMARY.md drafted with the final verification counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `astro.config.mjs` integrations pipeline → build-time HTML output | Changes to integration config can silently alter the class/structure of emitted HTML across every page type — both marketing (/, /posts/, /releases/) and Starlight (/docs/*). This is the single file the fix touches; the blast radius is "every page the site emits". |
| Local `npm ci` + Node 22.16.0 → Cloudflare Pages build runner | If the local reproduction does not match CF's build environment (H4), a fix that passes local verification could still fail on CF. Mitigated by the 02.1-05 checkpoint re-running on the actual CF preview — no gap-closure plan can make this disappear without that re-run. |
| `gsd/phase-2.1-blog` branch → `main` branch | This plan MUST NOT touch `main`. Production runs from `main`. Every commit in this plan lands on the feature branch only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-19 | Tampering | `astro.config.mjs` remediation path | mitigate | Atomic single-commit fix on feature branch only; Task 1 diagnosis before Task 2 edit prevents shotgun changes; Task 3 grep assertions validate the emitted HTML against both the regression target (/docs/* EC) AND the Wave 4 invariants (/docs/* and /posts/* alternate rel) so a fix that secretly regresses something else fails verification. |
| T-02.1-20 | Denial of Service | Starlight's bundled Expressive Code pipeline | mitigate | Task 1's Step 2 experiments isolate which configuration disables the pipeline; Task 2's remediation addresses the specific cause rather than a blanket workaround; Task 3 confirms the pipeline is back via grep assertions on the emitted HTML, not on the source config. |
| T-02.1-21 | Information Disclosure | RSS alternate-link URL on /docs/* (Wave 4 invariant) | accept in this plan; verified | The tag is intentional (feed discoverability). Threat Model from Plan 04 (T-02.1-16) covered this. This plan's job is to PRESERVE it while fixing EC — so the risk posture does not change; Task 3 explicitly verifies the tag still emits on /docs/* post-fix. |
| T-02.1-22 | Spoofing | Feature branch push → CF Pages preview | accept | CF Pages automatically builds previews for non-main branches; the preview URL is `*.pages.dev`, not `covalence.app`. The 02.1-05 re-run confirms the fix on the preview before any merge to `main`. No mitigation needed beyond "don't push `main`". |
| T-02.1-23 | Elevation of Privilege | Option H2-A (custom Starlight plugin) OR Option H2-B (component override) | mitigate | If Task 2 selects H2-A/H2-B and introduces new files (`src/components/StarlightHead.astro` or `src/starlight-plugins/*.ts`), those files have file-system scope identical to existing project files (build-time only, no runtime). Code review at Task 2's commit boundary covers the net-new code; the files are tiny (head-tag emission only). No `exec`, no `child_process`, no `fetch`. |

Block: T-02.1-19, T-02.1-20 — verification in Task 3 is a hard gate; a fix that fails the grep assertions is rejected.
Accept: T-02.1-21, T-02.1-22 (unchanged from Plan 04 threat posture), T-02.1-23 (bounded by Starlight's own plugin/component API surface).
</threat_model>

<verification>
After all three tasks land:

1. `npm run build` exits 0 on `gsd/phase-2.1-blog` at HEAD.
2. `grep -oE 'class="expressive-code"' dist/docs/getting-started/index.html | wc -l` returns ≥ `1`.
3. `grep -oE 'ec-line' dist/docs/getting-started/index.html | wc -l` returns ≥ `1`.
4. `grep -oE 'expressive-code|ec-line' dist/docs/getting-started/index.html | wc -l` returns ≥ `10` (matches the Wave 4 recorded `32` ±).
5. `grep -cE 'rel="alternate" type="application/rss\+xml"' dist/docs/getting-started/index.html` returns exactly `1` (Wave 4 invariant preserved).
6. `grep -cE 'rel="alternate" type="application/rss\+xml"' dist/posts/index.html` returns exactly `1`.
7. `dist/posts/rss.xml` parses as well-formed XML (`python3 -c "import xml.etree.ElementTree as ET; ET.parse('dist/posts/rss.xml')"` exits 0).
8. Exactly one fix commit exists on `gsd/phase-2.1-blog` with message pattern `fix(02.1-06): ...`.
9. The fix commit message OR `.planning/phases/2.1-blog/02.1-06-DIAGNOSIS.md` names the confirmed hypothesis (H1-H5) or states "inconclusive — defensive fix" with justification.
10. `main` branch has NOT been modified — `git log main --oneline -1` must predate this plan's commits.
11. `gsd/phase-2.1-blog` is pushed to origin (or push failure is flagged).
</verification>

<success_criteria>
- [ ] `npm run build` passes on `gsd/phase-2.1-blog` at HEAD.
- [ ] `/docs/getting-started/` HTML contains both `expressive-code` and `ec-line` class markers.
- [ ] `/docs/getting-started/` HTML still contains exactly one `<link rel="alternate" type="application/rss+xml">` (Wave 4 invariant).
- [ ] `/posts/index.html` still emits the alternate rel link via Base.astro.
- [ ] `/posts/rss.xml` is well-formed.
- [ ] Diagnostic evidence for H1-H5 recorded (in commit message or `02.1-06-DIAGNOSIS.md`).
- [ ] One atomic fix commit on `gsd/phase-2.1-blog` named `fix(02.1-06): ...`.
- [ ] No commits on `main`.
- [ ] Branch pushed; CF Pages preview rebuilds automatically; 02.1-05 checkpoint can now re-run.
- [ ] `02.1-06-SUMMARY.md` drafted.
</success_criteria>

<output>
After completion, create `.planning/phases/2.1-blog/02.1-06-SUMMARY.md` summarising:

- Which hypothesis was confirmed (or "inconclusive — defensive fix").
- The exact remediation applied (file diff summary).
- Final grep counts on `/docs/*` and `/posts/*` from Task 3.
- The Wave 4 invariants verified as preserved (RSS alternate rel on both surfaces).
- A one-line handoff to `/gsd-execute-phase 2.1 --gaps-only` → next step is 02.1-05 checkpoint re-run in a browser.

Also ensure `.planning/phases/2.1-blog/02.1-06-DIAGNOSIS.md` exists (or the equivalent content is visible in the fix commit's extended message) so future operators can see which hypothesis won.
</output>
