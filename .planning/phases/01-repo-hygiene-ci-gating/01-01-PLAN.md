---
phase: 01-repo-hygiene-ci-gating
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CONTRIBUTING.md
  - .github/pull_request_template.md
  - .github/ISSUE_TEMPLATE/bug_report.md
  - .github/ISSUE_TEMPLATE/content_typo.md
  - .github/ISSUE_TEMPLATE/config.yml
autonomous: true
requirements:
  - REPO-01
  - REPO-02
  - REPO-03
  - REPO-04
tags:
  - docs
  - repo-hygiene
  - github-templates

must_haves:
  truths:
    - "A first-time visitor to the repo root on GitHub can read README.md and go from 'what is this?' to a running local dev server without consulting any other source."
    - "README.md states the Node prerequisite (>=22.12.0, pinned 22.16.0 via .nvmrc) and shows install / dev / build / preview commands."
    - "README.md describes the Cloudflare Pages auto-deploy flow (push to main triggers a build) and links back to the main Covalence app repo (github.com/darronz/covalence)."
    - "CONTRIBUTING.md tells a new contributor how to branch, what commit-message style to use (Conventional Commits, matching existing git log), how the PR workflow works, and where docs vs. marketing content live."
    - "Opening a new PR on GitHub auto-fills a template asking what changed, why, and screenshots for UI changes, plus a contributor checklist."
    - "Opening a new issue on GitHub presents at minimum a Bug Report template and a Content / Typo Report template."
  artifacts:
    - path: "README.md"
      provides: "Repo-root onboarding doc"
      contains: "## Prerequisites"
      min_lines: 60
    - path: "CONTRIBUTING.md"
      provides: "Contributor workflow doc"
      contains: "Conventional Commits"
      min_lines: 60
    - path: ".github/pull_request_template.md"
      provides: "Auto-filled PR body"
      contains: "## What changed"
    - path: ".github/ISSUE_TEMPLATE/bug_report.md"
      provides: "Bug report issue template"
      contains: "name: Bug report"
    - path: ".github/ISSUE_TEMPLATE/content_typo.md"
      provides: "Content/typo issue template"
      contains: "name: Content / Typo"
    - path: ".github/ISSUE_TEMPLATE/config.yml"
      provides: "Disables blank issues, points to main app repo for product bugs"
      contains: "blank_issues_enabled"
  key_links:
    - from: "README.md"
      to: "CONTRIBUTING.md"
      via: "relative markdown link"
      pattern: "\\]\\(\\.?/?CONTRIBUTING\\.md\\)"
    - from: "README.md"
      to: "https://github.com/darronz/covalence"
      via: "absolute hyperlink to upstream app repo"
      pattern: "github\\.com/darronz/covalence"
    - from: "CONTRIBUTING.md"
      to: "src/content/docs/docs/ vs src/components/"
      via: "explicit 'where content lives' section"
      pattern: "src/content/docs/docs/"
---

<objective>
Make the repo self-explanatory to an outside contributor by adding the four repo-hygiene
documents the codebase is missing today (README.md, CONTRIBUTING.md, PR template, issue
templates). After this plan, a stranger landing on the GitHub repo page can answer
"what is this, how do I run it, how do I contribute?" using only the files in this
repo — no Slack, no DMs, no need to read source.

Purpose: Close REPO-01..04. The repo was carved out of the main Covalence app monorepo
in v1.3 Phase 12; it has never had its own README/CONTRIBUTING/templates. This plan
fixes that.

Output: Six new markdown/yaml files at the repo root and under .github/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/INTEGRATIONS.md
@CLAUDE.md
@package.json
@.nvmrc

<interfaces>
<!-- Source-of-truth values to use verbatim in the docs. Do NOT invent values. -->

From package.json:
- `"name": "website"`
- `"version": "0.0.1"` (do NOT mention this in README — see CONCERNS.md tech debt note)
- `"engines": { "node": ">=22.12.0" }`
- Scripts: `dev` (astro dev), `build` (astro build), `preview` (astro preview), `astro` (passthrough)
- Dependencies: `astro ^6.1.6`, `@astrojs/starlight ^0.38.3`

From .nvmrc:
- Pinned Node version: `22.16.0` (this is what CI and Cloudflare Pages use; reproducible builds)

From astro.config.mjs (for context — do NOT modify):
- `site: 'https://covalence.app'`
- `output: 'static'`
- Starlight integration mounted; sidebar manually configured

From git log (Conventional Commits style observed):
- `feat(scope): ...`, `chore(scope): ...`, `docs: ...`, `fix(scope): ...`, `chore(release): vX.Y.Z`
- Scope is optional but used when narrowing to a subsystem (e.g. `feat(hero):`, `feat(website):`, `chore(release):`).

Cross-repo coordinates:
- Main Covalence app repo: `https://github.com/darronz/covalence`
- This repo's deployed domain: `https://covalence.app`
- Cloudflare Pages project name: `covalence-website-v2` (Git-integrated)

Directory layout (CRITICAL — for CONTRIBUTING.md "where content lives" section):
- Marketing landing page: `src/pages/index.astro`
- Marketing layout: `src/layouts/Base.astro`
- Marketing components: `src/components/*.astro` (Nav, Hero, HowItWorks, Features, Architecture, Footer)
- Docs content: `src/content/docs/docs/*.md` (note the deliberate `docs/docs/` nesting)
- Sidebar config (must be edited when adding a doc page): `astro.config.mjs`
- Design tokens: `src/styles/global.css`
- Starlight theme overrides: `src/styles/starlight.css`
- Release data (machine-written, do not hand-edit): `src/data/latest-release.json`, `src/data/releases.json`
- Release dispatch handler: `.github/workflows/new-release.yml`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author README.md at repo root (REPO-01)</name>
  <files>README.md</files>
  <read_first>
    - README.md (verify it does not already exist; if it does, this task overwrites it)
    - package.json (Node engine, scripts)
    - .nvmrc (pinned version)
    - .planning/PROJECT.md (Core Value paragraph for the intro)
    - .planning/codebase/STACK.md (frameworks summary)
    - .planning/codebase/INTEGRATIONS.md (deploy story)
  </read_first>
  <action>
    Create README.md at the repo root. Use ATX-style headings (#) and 2-space-indented
    fenced code blocks. Match the conventions in .planning/codebase/CONVENTIONS.md:
    plain markdown, no Prettier-specific formatting, code fences use language hints.

    The file MUST contain the following sections, in this order, with the content
    described:

    1. `# Covalence Website`
       One-paragraph intro. Verbatim opening sentence: "The marketing, documentation,
       and releases site for [Covalence](https://github.com/darronz/covalence) — a
       native macOS app that gives any AI client persistent, local-first memory via
       MCP." Follow with one more sentence stating this is a standalone Astro 6 +
       Starlight static site deployed to https://covalence.app via Cloudflare Pages.

    2. `## What this repo is`
       2–3 sentences explaining: this is content + components only (no backend, no
       runtime); it is one side of a cross-repo contract — the main app repo dispatches
       `repository_dispatch` events of type `new-release` which a workflow here turns
       into JSON data files that drive the homepage. Link to the main app repo:
       `[github.com/darronz/covalence](https://github.com/darronz/covalence)`.

    3. `## Prerequisites`
       Bullet list:
       - Node.js `>=22.12.0` (this repo pins `22.16.0` via `.nvmrc` for reproducibility
         with Cloudflare Pages — recommended: install `nvm` and run `nvm use` from the
         repo root)
       - npm (ships with Node)
       - macOS, Linux, or Windows — no platform-specific build steps

    4. `## Install`
       Single fenced bash block:
       ```bash
       nvm use            # picks up .nvmrc -> 22.16.0
       npm ci             # reproducible install from package-lock.json
       ```
       One-sentence note: prefer `npm ci` over `npm install` so contributors and CI
       agree on the dependency tree.

    5. `## Local development`
       Fenced bash block with the three primary scripts and a one-line description
       each:
       ```bash
       npm run dev        # astro dev server at http://localhost:4321
       npm run build      # production build into dist/
       npm run preview    # serve dist/ locally to verify the built output
       ```
       Add a short note: there are no tests in this repo today; verification is
       `npm run build` succeeding plus a Cloudflare Pages preview deploy on PR.

    6. `## Project layout`
       A short tree (use a fenced text block) showing only the top-level dirs and
       the key authoring locations a contributor will touch:
       ```text
       src/
         pages/index.astro            # Marketing landing page (/)
         layouts/Base.astro           # Marketing layout
         components/*.astro           # Marketing sections (Hero, Features, ...)
         content/docs/docs/*.md       # Starlight docs (served at /docs/<slug>/)
         data/*.json                  # Release metadata (machine-written)
         styles/global.css            # Design tokens + base styles
         styles/starlight.css         # Starlight theme overrides
       public/                         # Static files served verbatim at site root
       astro.config.mjs                # Astro + Starlight config (sidebar lives here)
       .github/workflows/new-release.yml  # Ingests release events into src/data/
       ```
       One-sentence pointer: see CONTRIBUTING.md for the docs-vs-marketing decision
       tree when adding a new page.

    7. `## Deployment`
       Two short paragraphs:
       - Cloudflare Pages is connected to this repo (project `covalence-website-v2`).
         Every push to `main` triggers a production build of `npm run build` and
         deploys `dist/` to https://covalence.app. Pull requests get a preview deploy
         at a `*.pages.dev` URL.
       - Releases of the Covalence macOS app push their metadata to this repo via
         a `repository_dispatch` event handled by `.github/workflows/new-release.yml`,
         which commits updated `src/data/latest-release.json` and `src/data/releases.json`
         to `main`. That commit triggers the Pages build, so a new release goes live
         with no manual intervention here.

    8. `## Contributing`
       One short paragraph plus a link: see [CONTRIBUTING.md](./CONTRIBUTING.md) for
       branching, commit message style, the PR workflow, and where new docs vs.
       marketing content belongs.

    9. `## Related repos`
       Bullet list, one entry:
       - [`darronz/covalence`](https://github.com/darronz/covalence) — the main
         Covalence macOS app source. Releases there trigger rebuilds here.

    Constraints:
    - Do NOT include any secrets, API keys, or environment variables in examples
      (this repo has none — the site is 100% static).
    - Do NOT mention `npm test` (no tests exist).
    - Do NOT reference `package.json` `version` field (it's `0.0.1` and misleading;
      see CONCERNS.md tech debt).
    - Do NOT add a license badge or section (LICENSE is deferred to v2 per
      REPO-v2-01 in REQUIREMENTS.md).
    - Use the canonical Covalence app repo URL `https://github.com/darronz/covalence`
      (lowercase user, no trailing slash).
  </action>
  <verify>
    <automated>test -f README.md && grep -q "^# Covalence Website" README.md && grep -q "^## Prerequisites" README.md && grep -q "^## Install" README.md && grep -q "^## Local development" README.md && grep -q "^## Project layout" README.md && grep -q "^## Deployment" README.md && grep -q "^## Contributing" README.md && grep -q ">=22.12.0" README.md && grep -q "22.16.0" README.md && grep -q "npm ci" README.md && grep -q "npm run build" README.md && grep -q "github.com/darronz/covalence" README.md && grep -q "covalence.app" README.md && grep -q "CONTRIBUTING.md" README.md && [ "$(wc -l < README.md)" -ge 60 ]</automated>
  </verify>
  <acceptance_criteria>
    - File README.md exists at repo root.
    - Contains H1 `# Covalence Website` on line that begins with `# `.
    - Contains H2 sections: `## Prerequisites`, `## Install`, `## Local development`, `## Project layout`, `## Deployment`, `## Contributing`.
    - Contains literal strings `>=22.12.0`, `22.16.0`, `npm ci`, `npm run build`, `npm run dev`, `npm run preview`, `covalence.app`, `github.com/darronz/covalence`, `CONTRIBUTING.md`.
    - Does NOT contain literal string `npm test` (no tests exist).
    - Does NOT contain literal string `LICENSE` or `MIT License` (deferred to v2).
    - File is at least 60 non-empty lines.
  </acceptance_criteria>
  <done>
    README.md authored, all required sections present, all required literal strings present,
    automated verify command exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author CONTRIBUTING.md at repo root (REPO-02)</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md (verify it does not already exist)
    - README.md (the file from Task 1 — to align tone and avoid duplication)
    - .planning/codebase/CONVENTIONS.md (the de-facto code style)
    - .planning/codebase/STRUCTURE.md (where new code goes)
    - astro.config.mjs (the manually-edited sidebar — contributors must update it)
  </read_first>
  <action>
    Create CONTRIBUTING.md at the repo root. The file MUST contain the following
    sections, in this order:

    1. `# Contributing to Covalence Website`
       One paragraph: thanks, then point to README.md for setup. State that this doc
       covers branching, commit message style, the PR workflow, and where new content
       goes.

    2. `## Branching`
       Bullet list with concrete branch-name conventions matching what makes sense
       for a small static site:
       - Branch off `main` for every change.
       - Branch name format: `<type>/<short-kebab-summary>` where `<type>` is one of
         `feat`, `fix`, `docs`, `chore`, `refactor`. Examples: `feat/releases-page`,
         `fix/footer-link`, `docs/contributing-guide`, `chore/bump-astro`.
       - Keep branches short-lived; rebase on `main` before opening the PR if `main`
         has moved.

    3. `## Commit messages`
       State that this repo follows Conventional Commits, observed in the existing
       git history. Format:
       ```text
       <type>(<optional-scope>): <imperative summary>
       ```
       Bullet list of types in use:
       - `feat` — a user-visible feature
       - `fix` — a bug fix
       - `docs` — documentation only
       - `chore` — tooling, deps, repo housekeeping
       - `refactor` — internal restructuring with no behavior change
       - `chore(release): vX.Y.Z` — release ingestion commits, written by the
         `new-release.yml` workflow (do NOT author these by hand)

       Concrete examples drawn from this repo's actual history:
       - `feat(hero): read version and download size from latest-release.json`
       - `chore: pin Node version for CF Pages build determinism`
       - `docs: define v1 requirements`
       - `fix(website): make Starlight sidebar selected text dark on rose highlight`

       One-sentence note: scope is optional; use it when the change is clearly
       scoped to one component or surface (e.g. `feat(hero):`, `fix(footer):`,
       `docs(contributing):`).

    4. `## Pull request workflow`
       Numbered list:
       1. Open the PR against `main`.
       2. Fill in the PR template (it auto-loads from
          `.github/pull_request_template.md`).
       3. Wait for the GitHub Actions build check to pass — it runs `npm ci` and
          `npm run build` on a runner pinned to `.nvmrc`. **A failing build blocks
          merge** (the workflow is configured as a required status check on `main`).
       4. Cloudflare Pages will post a preview deploy URL on the PR — open it and
          spot-check anything visual.
       5. Squash-merge once green and reviewed.

       Add a short note: there are no tests in this repo today, so the build check
       and the Pages preview are the safety net. If you change anything visual,
       attach a before/after screenshot to the PR per the template.

    5. `## Where content lives`
       This section is the load-bearing one for REPO-02 — a contributor must be
       able to decide _without asking_ whether their change goes in marketing or
       docs. Use a bulleted decision tree:

       - **A new section on the marketing landing page (`/`)**: add a new
         `PascalCase.astro` component under `src/components/`, then import and place
         it inside `<main>` in `src/pages/index.astro`. Use design tokens from
         `src/styles/global.css` (`var(--accent)`, `var(--space-lg)`, the
         `.container`, `.section`, `.section-title` utilities).
       - **A new top-level marketing page (e.g. `/pricing`)**: add
         `src/pages/<route>.astro`, wrap it in `<Base title="…" description="…">`
         from `src/layouts/Base.astro`. Astro emits it at `/<route>/`.
       - **A new documentation page**: add `src/content/docs/docs/<slug>.md` with
         frontmatter (`title` and `description` are required by Starlight). Then
         add a matching entry to the `sidebar` array in `astro.config.mjs` —
         pages are NOT auto-added to the sidebar.
       - **An MDX doc using Starlight components (e.g. `<Tabs>`)**: name the file
         `.mdx` and import from `@astrojs/starlight/components`.
       - **A new design token (color, spacing)**: add to `:root` in
         `src/styles/global.css`. If it should also apply inside the docs theme,
         mirror it in `src/styles/starlight.css`.
       - **A new release-data field**: extend the dispatch payload in
           `.github/workflows/new-release.yml` and consume it in the relevant
           component. Do NOT hand-edit `src/data/latest-release.json` or
           `src/data/releases.json` — they are written by CI on
           `repository_dispatch` events.

    6. `## Code style`
       Short bullet list, lifted directly from `.planning/codebase/CONVENTIONS.md`:
       - 2-space indentation throughout.
       - Single quotes for JS/TS strings, double quotes for HTML attributes,
         semicolons in JS/TS.
       - Astro components use `PascalCase.astro`; pages use lowercase matching the
         URL segment; docs use `kebab-case.md`.
       - No Prettier / ESLint / Biome is configured — match what's already there.

    7. `## Reporting bugs and content issues`
       One sentence: open an issue using the appropriate template (Bug report or
       Content / Typo report). Bug reports about the Covalence app itself (the
       macOS product) belong in the upstream repo:
       [github.com/darronz/covalence/issues](https://github.com/darronz/covalence/issues).

    Constraints:
    - Do NOT invent CI behavior beyond what Plan 02 will deliver: `npm ci` +
      `npm run build` on PR, configured as a required status check.
    - Do NOT mention testing frameworks or `npm test` — none exist.
    - Do NOT reference any LICENSE.
    - Use exact Conventional Commit examples from this repo's `git log` (sample
      provided in <interfaces>) so the doc reflects observed reality, not aspiration.
  </action>
  <verify>
    <automated>test -f CONTRIBUTING.md && grep -q "^# Contributing to Covalence Website" CONTRIBUTING.md && grep -q "^## Branching" CONTRIBUTING.md && grep -q "^## Commit messages" CONTRIBUTING.md && grep -q "^## Pull request workflow" CONTRIBUTING.md && grep -q "^## Where content lives" CONTRIBUTING.md && grep -q "^## Code style" CONTRIBUTING.md && grep -q "Conventional Commits" CONTRIBUTING.md && grep -q "src/content/docs/docs/" CONTRIBUTING.md && grep -q "src/components/" CONTRIBUTING.md && grep -q "astro.config.mjs" CONTRIBUTING.md && grep -q "pull_request_template" CONTRIBUTING.md && grep -q "required status check" CONTRIBUTING.md && [ "$(wc -l < CONTRIBUTING.md)" -ge 60 ]</automated>
  </verify>
  <acceptance_criteria>
    - File CONTRIBUTING.md exists at repo root.
    - Contains H1 `# Contributing to Covalence Website`.
    - Contains H2 sections: `## Branching`, `## Commit messages`, `## Pull request workflow`, `## Where content lives`, `## Code style`.
    - Contains literal strings: `Conventional Commits`, `src/content/docs/docs/`, `src/components/`, `astro.config.mjs`, `pull_request_template`, `required status check`.
    - Contains at least one of the actual git-log commit examples (e.g. `feat(hero):` or `chore: pin Node`).
    - Does NOT contain literal `npm test`.
    - File is at least 60 non-empty lines.
  </acceptance_criteria>
  <done>
    CONTRIBUTING.md authored, all required sections present, automated verify exits 0.
    A new contributor can decide where to put a docs vs. marketing change without
    asking anyone.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author PR template and issue templates (REPO-03, REPO-04)</name>
  <files>.github/pull_request_template.md, .github/ISSUE_TEMPLATE/bug_report.md, .github/ISSUE_TEMPLATE/content_typo.md, .github/ISSUE_TEMPLATE/config.yml</files>
  <read_first>
    - .github/pull_request_template.md (verify it does not exist; this task creates it)
    - .github/ISSUE_TEMPLATE/ (verify directory does not exist; this task creates it)
    - .github/workflows/new-release.yml (for context on what already lives in .github/)
  </read_first>
  <action>
    Create four files. All paths are relative to the repo root.

    --- File 1: `.github/pull_request_template.md` (REPO-03) ---

    GitHub auto-loads this when a PR is opened. Use plain markdown (no frontmatter
    — this is the PR body template, not an issue form).

    Required sections (use these exact headings):

    ```markdown
    ## What changed

    <!-- A short description of the change. Bullet list is fine. -->

    ## Why

    <!-- The motivation. Link to the issue, requirement ID (e.g. REPO-01), or
         GSD plan if applicable. -->

    ## Screenshots (UI changes only)

    <!-- Before / after screenshots or a short screen recording for any change
         that affects what visitors see. Delete this section if the change has
         no visual impact. -->

    ## Checklist

    - [ ] Ran `npm run build` locally and it succeeded
    - [ ] Commit messages follow Conventional Commits (see CONTRIBUTING.md)
    - [ ] Updated docs / sidebar (`astro.config.mjs`) if a docs page was added
    - [ ] No secrets, API keys, or generated `dist/` files committed
    - [ ] If this changes the homepage or releases data, verified the Cloudflare
          Pages preview deploy renders correctly
    ```

    Do NOT include any auto-assignment metadata, reviewers, or labels in this file
    (that's repo-settings configuration, not template content).

    --- File 2: `.github/ISSUE_TEMPLATE/bug_report.md` (REPO-04, part 1) ---

    GitHub Markdown issue template format. Frontmatter is REQUIRED so GitHub picks
    it up as a chooser entry. Exact frontmatter:

    ```markdown
    ---
    name: Bug report
    about: Report a bug on the Covalence website (NOT the macOS app)
    title: "[Bug] "
    labels: ["bug"]
    ---

    ## What happened

    <!-- A clear and concise description of the bug as you observed it on
         https://covalence.app. -->

    ## What you expected

    <!-- What did you expect to see instead? -->

    ## Steps to reproduce

    1.
    2.
    3.

    ## Environment

    - URL where it happened (e.g. `https://covalence.app/docs/getting-started/`):
    - Browser + version:
    - OS:
    - Screen size / device (if relevant):

    ## Screenshots

    <!-- Drag and drop images here if useful. -->

    ## Notes

    <!-- For bugs in the Covalence macOS app itself, please file at
         https://github.com/darronz/covalence/issues instead. -->
    ```

    --- File 3: `.github/ISSUE_TEMPLATE/content_typo.md` (REPO-04, part 2) ---

    Markdown issue template for content / typo / wording reports.

    ```markdown
    ---
    name: Content / Typo report
    about: Report a typo, broken link, outdated content, or unclear wording on the site
    title: "[Content] "
    labels: ["content"]
    ---

    ## Where on the site

    <!-- Full URL, e.g. https://covalence.app/docs/getting-started/ -->

    ## What's wrong

    <!-- Quote the offending text or describe the issue. Screenshots help. -->

    ## Suggested fix

    <!-- Optional: propose the corrected wording or a fix. -->
    ```

    --- File 4: `.github/ISSUE_TEMPLATE/config.yml` ---

    Disables the blank-issue option (so users always pick a template) and adds a
    contact link pointing app-bug-reporters at the upstream repo.

    ```yaml
    blank_issues_enabled: false
    contact_links:
      - name: Bug in the Covalence macOS app
        url: https://github.com/darronz/covalence/issues
        about: For bugs in the Covalence app itself (not the website), please file at the main app repo.
    ```

    Constraints:
    - The .github/ISSUE_TEMPLATE directory must be created if it does not exist.
    - Each issue template file must have valid YAML frontmatter terminated by `---`.
    - Do NOT use the newer `.yml` issue-form schema for the bug/content templates;
      use the simpler markdown template format (frontmatter + free-form markdown
      body) — it is sufficient for this stage and lower-friction for contributors.
    - The `config.yml` file's `blank_issues_enabled: false` must be exactly that
      value (boolean false, not the string `"false"`).
  </action>
  <verify>
    <automated>test -f .github/pull_request_template.md && test -f .github/ISSUE_TEMPLATE/bug_report.md && test -f .github/ISSUE_TEMPLATE/content_typo.md && test -f .github/ISSUE_TEMPLATE/config.yml && grep -q "^## What changed" .github/pull_request_template.md && grep -q "^## Why" .github/pull_request_template.md && grep -q "^## Screenshots" .github/pull_request_template.md && grep -q "^## Checklist" .github/pull_request_template.md && grep -q "npm run build" .github/pull_request_template.md && grep -q "Conventional Commits" .github/pull_request_template.md && grep -q "^name: Bug report" .github/ISSUE_TEMPLATE/bug_report.md && grep -q "^name: Content / Typo report" .github/ISSUE_TEMPLATE/content_typo.md && grep -q "blank_issues_enabled: false" .github/ISSUE_TEMPLATE/config.yml && grep -q "github.com/darronz/covalence/issues" .github/ISSUE_TEMPLATE/config.yml</automated>
  </verify>
  <acceptance_criteria>
    - File `.github/pull_request_template.md` exists with H2 sections `## What changed`, `## Why`, `## Screenshots`, `## Checklist`, and a checklist item mentioning `npm run build` and one mentioning `Conventional Commits`.
    - File `.github/ISSUE_TEMPLATE/bug_report.md` exists with frontmatter line `name: Bug report` and includes sections for reproduction steps and environment.
    - File `.github/ISSUE_TEMPLATE/content_typo.md` exists with frontmatter line `name: Content / Typo report`.
    - File `.github/ISSUE_TEMPLATE/config.yml` exists with `blank_issues_enabled: false` and a `contact_links` entry whose URL contains `github.com/darronz/covalence/issues`.
    - All four files are valid (yaml frontmatter terminates with `---`; yaml file is parseable as YAML).
  </acceptance_criteria>
  <done>
    PR template and two issue templates plus the config.yml exist under .github/.
    Opening a new PR on GitHub auto-fills the body with the four sections; opening
    a new issue presents the chooser with two named templates and no blank-issue
    option.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public repo on GitHub | All files in this plan are world-readable. No secrets must appear in any document or example. |
| Issue/PR contributor input | Issue and PR template _bodies_ are filled by external contributors at runtime — but the templates themselves do not execute anything; GitHub renders them as plain markdown. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Information disclosure | README.md, CONTRIBUTING.md | mitigate | No secrets, API keys, dispatch tokens (`WEBSITE_DISPATCH_TOKEN`), or env-var values appear in any code example. The site is fully static and consumes no env vars; this is enforced by acceptance_criteria's negative grep on sensitive patterns being absent in author guidance. |
| T-01-01-02 | Tampering | .github/ISSUE_TEMPLATE/config.yml | mitigate | The `contact_links` URL is a literal string `https://github.com/darronz/covalence/issues` — no interpolation, no template variables, no fetch. GitHub renders it as a static external link in the issue chooser. |
| T-01-01-03 | Repudiation | Conventional commit guidance in CONTRIBUTING.md | accept | Commit-message style is documentation, not enforcement. A future enhancement could add a commit-lint check, but for v1 this is informational only and the cost of accepting noisy history on this small site is minimal. |
| T-01-01-04 | Spoofing | Issue templates inviting external content | accept | Issue templates themselves cannot be used to spoof identity — GitHub authenticates the issue author. The `Notes` section in bug_report.md tells users to file app bugs at the upstream repo, but does not validate that they did; this is a UX hint, not a security boundary. |
</threat_model>

<verification>
After all three tasks complete, run from the repo root:

```bash
test -f README.md && test -f CONTRIBUTING.md && test -f .github/pull_request_template.md && test -f .github/ISSUE_TEMPLATE/bug_report.md && test -f .github/ISSUE_TEMPLATE/content_typo.md && test -f .github/ISSUE_TEMPLATE/config.yml && echo "all six files present"
```

Spot-check rendering by opening README.md and CONTRIBUTING.md in any markdown viewer
(VS Code preview, GitHub web UI on the PR) — headings should be hierarchical, code
blocks should be fenced and syntax-highlighted.

Verify YAML frontmatter on the issue templates parses cleanly:

```bash
node -e "const fs=require('fs'); const yaml=fs.readFileSync('.github/ISSUE_TEMPLATE/config.yml','utf8'); console.log(yaml);"
```

(No external YAML parser is available in this repo's deps; visual inspection plus
the grep-based acceptance_criteria are the verification path.)

The plan does NOT change `astro build` behavior, so a `npm run build` smoke check is
not strictly required — but running it is cheap and confirms the new files don't
accidentally break the build (they shouldn't; none of them are under `src/` or
`public/`).
</verification>

<success_criteria>
1. README.md, CONTRIBUTING.md, .github/pull_request_template.md, .github/ISSUE_TEMPLATE/bug_report.md, .github/ISSUE_TEMPLATE/content_typo.md, .github/ISSUE_TEMPLATE/config.yml all exist at the paths declared in `files_modified`.
2. Each file's automated `<verify>` command exits 0.
3. README.md covers prerequisites, install, dev, build, preview, deployment, links to CONTRIBUTING.md, and links to the main Covalence app repo (REPO-01 ✓).
4. CONTRIBUTING.md covers branching, commit-message style (Conventional Commits with examples from this repo's git log), PR workflow, and where docs vs. marketing content lives (REPO-02 ✓).
5. PR template asks for what changed, why, and screenshots for UI changes (REPO-03 ✓).
6. Issue chooser offers Bug report + Content/Typo report templates and disables blank issues (REPO-04 ✓).
</success_criteria>

<output>
After completion, create `.planning/phases/01-repo-hygiene-ci-gating/01-01-SUMMARY.md`
following @$HOME/.claude/get-shit-done/templates/summary.md, listing the six files
created and confirming each requirement (REPO-01 .. REPO-04) is satisfied.
</output>
