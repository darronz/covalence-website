---
plan_id: 03-03
phase: 03-content-depth-seo
plan: 03
title: Architecture.astro arch-stack rewrite — 2-3 paragraph retrieval teaser + CTA to /docs/under-the-hood/
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - src/components/Architecture.astro
autonomous: true
requirements: [CONT-01]

must_haves:
  truths:
    - "Visitors to the home page see a 2-3 paragraph prose teaser of Covalence's retrieval stack in the left column of the 'Under the hood' section — not the old five-bullet list"
    - "The teaser links via an inline 'Full technical deep-dive' CTA to /docs/under-the-hood/ (trailing slash) — clicking the link lands on the page authored in Plan 03-02"
    - "The right-hand 'Privacy by architecture' column (arch-privacy) is byte-identical to its pre-phase state — same heading, same two paragraphs, same CSS classes"
    - "The rewrite does not use 'CoreML on Apple Neural Engine', 'instant', 'free', 'millisecond inference', or any other promotional adjective"
  artifacts:
    - path: "src/components/Architecture.astro"
      provides: "Marketing retrieval-stack teaser (arch-stack column) + unchanged privacy column + unchanged style block scoped to surviving selectors"
      contains: "/docs/under-the-hood/"
  key_links:
    - from: "src/components/Architecture.astro .arch-stack"
      to: "src/content/docs/docs/under-the-hood.md (Plan 03-02)"
      via: "<a href='/docs/under-the-hood/'> CTA"
      pattern: "href=\"/docs/under-the-hood/\""
    - from: "src/components/Architecture.astro .arch-privacy"
      to: "unchanged voice-anchor column"
      via: "self-analog preservation"
      pattern: "Your data never leaves your machine"
---

<objective>
Rewrite the `arch-stack` column of `src/components/Architecture.astro` on the home page from its current 5-bullet list into a 2-3 paragraph prose teaser that introduces Covalence's retrieval stack at marketing altitude and links into the deep-dive page at `/docs/under-the-hood/` (created in Plan 03-02).

Purpose: Close the CONT-01 marketing-surface half. Success Criterion 1 ("a technical reader can, on a single page of the site, read a concrete description...") is satisfied by the deep page; this plan gives casual visitors enough to decide whether the depth is worth clicking into, using the same voice cadence as the `arch-privacy` sibling column.

Output: `src/components/Architecture.astro` with lines 5-14 (the current `arch-stack` div) replaced by a prose teaser + CTA; lines 15-25 (the entire `arch-privacy` div) byte-identical; the `<style>` block trimmed of now-dead `.arch-stack ul|li|li::before|li strong` rules and augmented with rules for the new paragraph/CTA classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-content-depth-seo/03-CONTEXT.md
@.planning/phases/03-content-depth-seo/03-RESEARCH.md
@.planning/phases/03-content-depth-seo/03-PATTERNS.md
@.planning/phases/03-content-depth-seo/03-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Current Architecture.astro structure (full file read at plan time) -->

### Surviving structure (must NOT change)

Lines 1-4: `<section class="section"><div class="container arch-inner"><h2 class="section-title">Under the hood</h2><div class="arch-grid">`
Lines 15-25: entire `.arch-privacy` div — h3 "Privacy by architecture" + `.privacy-statement` + `.privacy-detail`
Lines 26-28: closing tags for `.arch-grid`, `.container`, `.section`
Lines 37-42: `.arch-stack h3, .arch-privacy h3 { ... }` — selector covers BOTH columns, survives
Lines 70-82: `.privacy-statement` + `.privacy-detail` CSS rules — survive unchanged
Lines 84-88: mobile breakpoint collapsing to single column — survives

### Replacement target (lines 5-14)

```astro
      <div class="arch-stack">
        <h3>The stack</h3>
        <ul>
          <li><strong>Database:</strong> SQLite + sqlite-vec — single file, embedded, battle-tested</li>
          <li><strong>Embeddings:</strong> CoreML on Apple Neural Engine — on-device, instant, free</li>
          <li><strong>Search:</strong> Hybrid vector + FTS5 keyword, merged via Reciprocal Rank Fusion</li>
          <li><strong>Protocol:</strong> MCP (stdio) — the standard for AI tool integration</li>
          <li><strong>UI:</strong> Native SwiftUI — belongs on your Mac</li>
        </ul>
      </div>
```

### Dead CSS rules to remove (lines 44-68)

```css
.arch-stack ul          { list-style: none; display: flex; flex-direction: column; gap: var(--space-sm); }
.arch-stack li          { color: var(--text-secondary); font-size: 0.9375rem; line-height: 1.5; padding-left: var(--space-md); position: relative; }
.arch-stack li::before  { content: '→'; position: absolute; left: 0; color: var(--accent-subtle); }
.arch-stack li strong   { color: var(--text-primary); }
```

After the rewrite there are no `<ul>`/`<li>` elements in `.arch-stack` — these four rules become dead code and should be removed.

### Voice anchor (the sibling column to mirror)

```astro
      <div class="arch-privacy">
        <h3>Privacy by architecture</h3>
        <p class="privacy-statement">
          Your data never leaves your machine. No accounts. No telemetry. No cloud. Runs entirely on-device.
        </p>
        <p class="privacy-detail">
          Every component — the database, the embedding model, the search engine — runs locally.
          There are no network calls, no API keys, no external dependencies.
          Your memories are a single SQLite file on your Mac that you own, back up, and export whenever you want.
        </p>
      </div>
```

Pattern: `<h3>` + one short "statement" paragraph (1 sentence, denial-cascade rhythm acceptable) + one longer "detail" paragraph (2-4 sentences, concrete specifics — component names, file types, numbers).

### CTA target

`/docs/under-the-hood/` — with trailing slash, matching the site-wide internal-doc-link convention (see `src/content/docs/docs/getting-started.md:81` for the pattern).

### CTA link text — CHOOSE ONE (planner-recommended: the first)

1. `Full technical deep-dive →` — matches the 03-CONTEXT.md D-02 wording ("inline 'Full technical deep-dive' CTA")
2. `Read the technical deep-dive →`
3. `How it actually works →`

Option 1 is the most consistent with the CONTEXT decision and the sibling privacy column's register. Use it unless there's a voice-specific reason to swap. The `→` arrow is consistent with the existing `.arch-stack li::before` arrow glyph (which is being removed; re-using the glyph in the CTA preserves a visual cue).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the arch-stack column with a 2-3 paragraph teaser + CTA; remove dead CSS rules; leave arch-privacy byte-identical</name>
  <files>src/components/Architecture.astro</files>
  <read_first>
    - src/components/Architecture.astro (full file — 89 lines; confirms current structure before editing)
    - .planning/phases/03-content-depth-seo/03-PATTERNS.md §Architecture.astro (landmines L1-L6 + Option B CSS strategy)
    - .planning/phases/03-content-depth-seo/03-RESEARCH.md §Voice/Tone Anchors (Hero/privacy/HowItWorks cadence)
    - .planning/phases/03-content-depth-seo/03-RESEARCH.md §Retrieval Stack Parameter Table (so the teaser names the same components at marketing altitude — 256 dims, MLTensor, RRF, recency 10%)
    - .planning/phases/03-content-depth-seo/03-02-SUMMARY.md (confirms /docs/under-the-hood/ exists and CTA target is live)
  </read_first>
  <action>
Edit `src/components/Architecture.astro`. Three distinct changes, all in this one file.

### Change 1 — REPLACE lines 5-14 (the `arch-stack` div)

Delete the current block:

```astro
      <div class="arch-stack">
        <h3>The stack</h3>
        <ul>
          <li><strong>Database:</strong> SQLite + sqlite-vec — single file, embedded, battle-tested</li>
          <li><strong>Embeddings:</strong> CoreML on Apple Neural Engine — on-device, instant, free</li>
          <li><strong>Search:</strong> Hybrid vector + FTS5 keyword, merged via Reciprocal Rank Fusion</li>
          <li><strong>Protocol:</strong> MCP (stdio) — the standard for AI tool integration</li>
          <li><strong>UI:</strong> Native SwiftUI — belongs on your Mac</li>
        </ul>
      </div>
```

Replace with the following 2-3 paragraph teaser. The `<h3>` text, the two paragraph classes, and the CTA `<p>` class are specified exactly; the prose INSIDE the paragraphs is flexible within the voice constraints — but below is a direct recommendation the executor can use verbatim. It matches the `arch-privacy` sibling-column cadence (one short statement paragraph + one longer detail paragraph) and then adds a CTA line:

```astro
      <div class="arch-stack">
        <h3>How retrieval works</h3>
        <p class="arch-teaser">
          Every memory becomes a 256-dimension vector. Search runs on-device. Vector similarity meets keyword match via RRF. No cloud round-trips.
        </p>
        <p class="arch-teaser-detail">
          Covalence embeds each memory with nomic-embed-text-v1.5 via MLTensor on Apple Silicon, truncates the output to 256 dimensions, and stores it in SQLite alongside an FTS5 full-text index.
          Searches hit both indexes in parallel — vector similarity and BM25 keyword scoring — and merge the results with Reciprocal Rank Fusion at k=60.
          A hyperbolic recency factor then nudges newer memories upward, weighted at 10% of the final score so recency never overrides genuine relevance.
        </p>
        <p class="arch-teaser-cta">
          <a href="/docs/under-the-hood/">Full technical deep-dive →</a>
        </p>
      </div>
```

Voice audit (pre-ship checklist — run against whatever final prose you land on):
- No `instant`, `amazing`, `powerful`, `blazing`, `lightning`, `free`, `seamless`, `revolutionary`, `effortless`, `cutting-edge` (Pitfall L3).
- No `CoreML on Apple Neural Engine` — use `MLTensor on Apple Silicon` (Pitfall L2).
- Numbers preferred over qualifiers: `256 dimensions`, `k=60`, `10%` are all more specific than "small", "standard", "lightly-weighted".
- Denial-cascade rhythm present in the short paragraph (`No cloud round-trips.`) — echoes `arch-privacy:18`'s "No accounts. No telemetry. No cloud.".
- CTA link target: `/docs/under-the-hood/` with trailing slash (Pitfall L4).

### Change 2 — DELETE lines 44-68 (the four dead `.arch-stack ul/li` CSS rules)

After the rewrite there are no `<ul>` or `<li>` elements in `.arch-stack`, so these rules become dead code. Remove all four rule blocks:

```css
  .arch-stack ul {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .arch-stack li {
    color: var(--text-secondary);
    font-size: 0.9375rem;
    line-height: 1.5;
    padding-left: var(--space-md);
    position: relative;
  }

  .arch-stack li::before {
    content: '→';
    position: absolute;
    left: 0;
    color: var(--accent-subtle);
  }

  .arch-stack li strong {
    color: var(--text-primary);
  }
```

### Change 3 — ADD three new CSS rules for the teaser paragraphs + CTA

After the surviving `.arch-stack h3, .arch-privacy h3` block (lines 37-42) — or, equivalently, anywhere in the `<style>` block below the `.arch-grid` rule — add rules that mirror the `.privacy-statement` / `.privacy-detail` typography (so the two columns feel like siblings), plus a new CTA rule:

```css
  .arch-teaser {
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: var(--space-md);
    line-height: 1.5;
  }

  .arch-teaser-detail {
    color: var(--text-secondary);
    font-size: 0.9375rem;
    line-height: 1.6;
    margin-bottom: var(--space-md);
  }

  .arch-teaser-cta {
    font-size: 0.9375rem;
    line-height: 1.5;
  }

  .arch-teaser-cta a {
    color: var(--accent-subtle);
    text-decoration: none;
    font-weight: 600;
  }

  .arch-teaser-cta a:hover,
  .arch-teaser-cta a:focus-visible {
    text-decoration: underline;
  }
```

If `--accent-subtle` doesn't produce enough contrast against the page background, fall back to `var(--text-primary)` with `border-bottom: 1px solid var(--accent-subtle)` — but try `--accent-subtle` first (it's what the old `.arch-stack li::before` arrow glyph used, so the visual continuity is intentional).

Phase 4's a11y sweep will audit link contrast globally; do NOT hand-roll a WCAG check here beyond picking a token that visibly renders.

### Change 4 — VERIFY arch-privacy + surrounding scaffolding are byte-identical

After all edits, confirm:

- Lines 1-4 unchanged: `<section class="section">...<div class="arch-grid">`
- Lines corresponding to the ORIGINAL 15-25 (the entire `.arch-privacy` div with h3 + `.privacy-statement` + `.privacy-detail` paragraphs) — byte-identical to pre-edit
- Closing tags for `.arch-grid`, `.container`, `.section` — unchanged
- `.arch-stack h3, .arch-privacy h3 { ... }` rule (currently lines 37-42) — unchanged (covers the new `<h3>How retrieval works</h3>` too)
- `.privacy-statement` and `.privacy-detail` CSS rules (currently lines 70-82) — unchanged
- `.arch-grid` rule (currently lines 31-35) — unchanged
- Mobile breakpoint `@media (max-width: 768px)` (currently lines 84-88) — unchanged
- `.arch-inner` class (if defined elsewhere) — unchanged

### Formatting invariants (CLAUDE.md conventions)

- 2-space indent
- Double quotes on HTML attributes (matches current file)
- No trailing whitespace
- Astro component pattern: plain HTML + `<style>` block, no frontmatter `---` section needed (the current file has no JS frontmatter)
  </action>
  <verify>
    <automated>
# === SURVIVAL CHECKS (arch-privacy + scaffolding must be intact) ===
grep -qF 'Your data never leaves your machine. No accounts. No telemetry. No cloud. Runs entirely on-device.' src/components/Architecture.astro &&
grep -qF 'Privacy by architecture'                                                                            src/components/Architecture.astro &&
grep -qF 'class="arch-privacy"'                                                                               src/components/Architecture.astro &&
grep -qF 'class="privacy-statement"'                                                                          src/components/Architecture.astro &&
grep -qF 'class="privacy-detail"'                                                                             src/components/Architecture.astro &&
grep -qF 'class="arch-grid"'                                                                                  src/components/Architecture.astro &&
grep -qF 'Every component — the database, the embedding model, the search engine — runs locally.'             src/components/Architecture.astro &&
grep -qF '.arch-stack h3,'                                                                                    src/components/Architecture.astro &&
grep -qF '.privacy-statement {'                                                                               src/components/Architecture.astro &&
grep -qF '@media (max-width: 768px)'                                                                          src/components/Architecture.astro &&

# === OLD ARCH-STACK COLUMN MUST BE GONE ===
! grep -qF 'CoreML on Apple Neural Engine'                                                                    src/components/Architecture.astro &&
! grep -qF 'on-device, instant, free'                                                                         src/components/Architecture.astro &&
! grep -qF '<strong>Database:</strong>'                                                                       src/components/Architecture.astro &&
! grep -qF '<strong>Embeddings:</strong>'                                                                     src/components/Architecture.astro &&
! grep -qF '<strong>Protocol:</strong>'                                                                       src/components/Architecture.astro &&
! grep -qF '<strong>UI:</strong>'                                                                             src/components/Architecture.astro &&
! grep -qE '\.arch-stack ul'                                                                                  src/components/Architecture.astro &&
! grep -qE '\.arch-stack li'                                                                                  src/components/Architecture.astro &&
! grep -qE '\.arch-stack li::before'                                                                          src/components/Architecture.astro &&

# === NEW ARCH-STACK COLUMN PRESENT ===
grep -qF 'class="arch-stack"'                                                                                 src/components/Architecture.astro &&
grep -qF 'class="arch-teaser"'                                                                                src/components/Architecture.astro &&
grep -qF 'class="arch-teaser-detail"'                                                                         src/components/Architecture.astro &&
grep -qF 'class="arch-teaser-cta"'                                                                            src/components/Architecture.astro &&
grep -qF 'href="/docs/under-the-hood/"'                                                                       src/components/Architecture.astro &&
grep -qE 'MLTensor|nomic-embed-text-v1\.5'                                                                    src/components/Architecture.astro &&
grep -qF '256'                                                                                                src/components/Architecture.astro &&
grep -qE 'RRF|Reciprocal Rank Fusion'                                                                         src/components/Architecture.astro &&

# === NEW CSS RULES PRESENT ===
grep -qF '.arch-teaser {'                                                                                     src/components/Architecture.astro &&
grep -qF '.arch-teaser-detail {'                                                                              src/components/Architecture.astro &&
grep -qF '.arch-teaser-cta {'                                                                                 src/components/Architecture.astro &&
grep -qF '.arch-teaser-cta a'                                                                                 src/components/Architecture.astro &&

# === VOICE INVARIANTS ===
! grep -qwi 'instant'                                                                                         src/components/Architecture.astro &&
! grep -qwi 'amazing'                                                                                         src/components/Architecture.astro &&
! grep -qwi 'powerful'                                                                                        src/components/Architecture.astro &&
! grep -qwi 'blazing'                                                                                         src/components/Architecture.astro &&
! grep -qwi 'lightning'                                                                                       src/components/Architecture.astro &&
! grep -qwi 'seamless'                                                                                        src/components/Architecture.astro &&
! grep -qwi 'effortless'                                                                                      src/components/Architecture.astro &&

# === BUILD + RENDER CHECK ===
npm run build 2>&1 | tee /tmp/plan-03-03-build.log &&
test -f dist/index.html &&
grep -qF 'class="arch-teaser"'                              dist/index.html &&
grep -qF 'href="/docs/under-the-hood/"'                     dist/index.html &&
grep -qE 'MLTensor|nomic-embed-text-v1\.5'                  dist/index.html &&
grep -qF 'Your data never leaves your machine'              dist/index.html &&
# The CTA resolves: /docs/under-the-hood/ exists in the built output
test -f dist/docs/under-the-hood/index.html
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -qF 'Your data never leaves your machine. No accounts. No telemetry. No cloud. Runs entirely on-device.' src/components/Architecture.astro` exits 0 (arch-privacy statement paragraph byte-identical)
    - `grep -qF 'Every component — the database, the embedding model, the search engine — runs locally.' src/components/Architecture.astro` exits 0 (arch-privacy detail paragraph byte-identical)
    - `grep -qF 'CoreML on Apple Neural Engine' src/components/Architecture.astro` exits 1 (old wording gone)
    - `grep -qF 'on-device, instant, free' src/components/Architecture.astro` exits 1 (promotional tail gone)
    - `grep -qE '<strong>(Database|Embeddings|Search|Protocol|UI):</strong>' src/components/Architecture.astro` exits 1 (old bullet list gone)
    - `grep -qE '\.arch-stack (ul|li)' src/components/Architecture.astro` exits 1 (dead CSS removed)
    - `grep -qF 'class="arch-teaser"' src/components/Architecture.astro` exits 0 (new statement paragraph present)
    - `grep -qF 'class="arch-teaser-detail"' src/components/Architecture.astro` exits 0 (new detail paragraph present)
    - `grep -qF 'class="arch-teaser-cta"' src/components/Architecture.astro` exits 0 (new CTA paragraph present)
    - `grep -qF 'href="/docs/under-the-hood/"' src/components/Architecture.astro` exits 0 (CTA link with trailing slash)
    - `grep -qE 'MLTensor|nomic-embed-text-v1\.5' src/components/Architecture.astro` exits 0 (accurate embedding-runtime wording present)
    - `grep -qE 'RRF|Reciprocal Rank Fusion' src/components/Architecture.astro` exits 0 (retrieval mechanism named)
    - `grep -qF '256' src/components/Architecture.astro` exits 0 (dimension number present)
    - `grep -qF '.arch-teaser {' src/components/Architecture.astro` exits 0 (new CSS rule for statement paragraph)
    - `grep -qF '.arch-teaser-detail {' src/components/Architecture.astro` exits 0 (new CSS rule for detail paragraph)
    - `grep -qF '.arch-teaser-cta {' src/components/Architecture.astro` exits 0 (new CSS rule for CTA paragraph)
    - Zero case-insensitive whole-word occurrences of `instant`, `amazing`, `powerful`, `blazing`, `lightning`, `seamless`, `effortless`
    - `.arch-grid`, `.privacy-statement`, `.privacy-detail`, `.arch-stack h3, .arch-privacy h3`, and `@media (max-width: 768px)` CSS rules all still present (verified by grep above)
    - `npm run build` exits 0 and writes `dist/index.html`
    - `dist/index.html` contains `class="arch-teaser"` AND `href="/docs/under-the-hood/"` AND `class="arch-privacy"`
    - `dist/docs/under-the-hood/index.html` exists (CTA target resolves — Plan 03-02 dependency satisfied)
    - Diff scope sanity: `git diff --stat src/components/Architecture.astro` reports a net change within roughly -30 to +30 lines (dead CSS removed, new prose + CSS added). A diff touching significantly more than that is likely collateral to arch-privacy or style rules outside scope.
  </acceptance_criteria>
  <done>Architecture.astro arch-stack column rewritten into a 2-3 paragraph teaser + CTA targeting /docs/under-the-hood/; four dead .arch-stack ul/li CSS rules removed; three new .arch-teaser* CSS rules added; arch-privacy column and all shared scaffolding byte-identical; npm run build exits 0; home page renders with the new teaser and the CTA target exists.</done>
</task>

</tasks>

<threat_model>
N/A — static marketing component. No user input, no auth, no data handling. The only new outbound link is an internal site-relative `/docs/under-the-hood/` — no external URL surface added.
</threat_model>

<verification>
1. `npm run build` exits 0.
2. `dist/index.html` contains the new `.arch-teaser` / `.arch-teaser-detail` / `.arch-teaser-cta` elements and the CTA href `/docs/under-the-hood/`.
3. `dist/index.html` still contains the unchanged `.arch-privacy` statement ("Your data never leaves your machine. No accounts. No telemetry. No cloud. Runs entirely on-device.").
4. `dist/docs/under-the-hood/index.html` is reachable (CTA resolves — satisfied by Plan 03-02 landing in Wave 1 before this Wave 2 plan).
5. No promotional adjectives present on the home page around the arch-stack region.
6. Manual preview-deploy visual check (deferred to Plan 03-04's checkpoint): the two columns still balance visually on desktop, and the mobile breakpoint still collapses cleanly to single-column.
</verification>

<success_criteria>
- Visitors to / see a 2-3 paragraph prose teaser in the left "Under the hood" column, with an inline CTA to /docs/under-the-hood/
- arch-privacy column is preserved byte-identical
- No promotional adjectives present
- CTA resolves to the deep page created in Plan 03-02
- CSS is trimmed (dead .arch-stack ul/li rules removed) and augmented (new .arch-teaser* rules added) with no collateral edits to unrelated selectors
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-depth-seo/03-03-SUMMARY.md` using the template. Record:
- Final prose chosen for the three paragraphs (verbatim) — so Plan 03-04's preview checkpoint knows what to look for on screen
- CTA link text chosen (default: "Full technical deep-dive →")
- Diff stats for src/components/Architecture.astro (lines added/removed)
- Confirmation that `.arch-privacy` was NOT touched (Pitfall L1 survived)
- Any deviations from the plan (should be none; if any, record Rule number)
</output>
