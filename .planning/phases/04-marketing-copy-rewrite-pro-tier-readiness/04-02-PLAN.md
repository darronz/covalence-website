---
phase: 04-marketing-copy-rewrite-pro-tier-readiness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/components/HowItWorks.astro]
autonomous: true
requirements: [COPY-05]

must_haves:
  truths:
    - "HowItWorks section-subtitle reads 'Add the MCP snippet. Paste the behavioural prompt. Done.' — tightened for power users"
    - "No banned voice words appear in the rendered HowItWorks section of dist/index.html"
    - "The section heading 'Two copy-pastes. That''s it.', both code blocks, and all CSS are byte-identical to pre-edit"
  artifacts:
    - path: "src/components/HowItWorks.astro"
      provides: "Tightened sub-copy for power-user audience"
      contains: "Add the MCP snippet. Paste the behavioural prompt. Done."
  key_links:
    - from: "src/components/HowItWorks.astro"
      to: "dist/index.html"
      via: "Astro SSG build"
      pattern: "Add the MCP snippet"
---

<objective>
Replace the HowItWorks.astro section subtitle (line 4) with the locked target string from D-04, tightening the sub-copy for a power-user audience.

Purpose: COPY-05 requires the "Two copy-pastes" sub-copy to be no more than one sentence tightened for a power-user audience. The current copy explains what MCP is; the target copy assumes the reader already knows.

Output: Modified `src/components/HowItWorks.astro` with the new subtitle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-marketing-copy-rewrite-pro-tier-readiness/04-CONTEXT.md

<interfaces>
<!-- Current HowItWorks.astro line 4 (the ONLY line this plan touches): -->
```html
    <p class="section-subtitle">Connect your AI client to Covalence's MCP server, then tell it how to use memory.</p>
```

<!-- Target replacement (D-04, verbatim): -->
```html
    <p class="section-subtitle">Add the MCP snippet. Paste the behavioural prompt. Done.</p>
```

<!-- Lines that MUST NOT change: -->
Line 1: `<section id="how-it-works" class="section">`
Line 3: `<h2 class="section-title">Two copy-pastes. That's it.</h2>`
Lines 5-37: Both code blocks (Connect step + Instruct step) — completely unchanged
Lines 40-116: All CSS unchanged
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace HowItWorks.astro subtitle with D-04 target string</name>
  <files>src/components/HowItWorks.astro</files>
  <read_first>
    - src/components/HowItWorks.astro (current state — confirm line 4 still reads "Connect your AI client to Covalence's MCP server, then tell it how to use memory.")
    - .planning/phases/04-marketing-copy-rewrite-pro-tier-readiness/04-CONTEXT.md (D-04 locked target string)
  </read_first>
  <action>
Edit `src/components/HowItWorks.astro` line 4. Replace:

```html
    <p class="section-subtitle">Connect your AI client to Covalence's MCP server, then tell it how to use memory.</p>
```

With:

```html
    <p class="section-subtitle">Add the MCP snippet. Paste the behavioural prompt. Done.</p>
```

This is a single-line replacement. Do NOT touch any other line in the file. The heading "Two copy-pastes. That's it." (line 3), both code blocks (lines 5-37), and all CSS (lines 40-116) remain byte-identical.

Per D-04: the current copy is an explainer ("Connect your AI client to Covalence's MCP server, then tell it how to use memory"). The target copy is imperative and assumes the reader already understands MCP: "Add the MCP snippet. Paste the behavioural prompt. Done." Note British spelling: "behavioural" (not "behavioral").
  </action>
  <verify>
    <automated>
set -e
# Positive: new string present
grep -qF 'Add the MCP snippet. Paste the behavioural prompt. Done.' src/components/HowItWorks.astro
# Negative: old string gone
! grep -qF "Connect your AI client to Covalence's MCP server" src/components/HowItWorks.astro
# Collateral check: heading unchanged
grep -qF "Two copy-pastes. That's it." src/components/HowItWorks.astro
# Collateral check: code blocks unchanged
grep -qF 'cov-mcp' src/components/HowItWorks.astro
grep -qF 'Covalence, a persistent' src/components/HowItWorks.astro
# Build check
npm run build
# Voice invariants on rendered output (D-13)
! grep -qwi 'instant' dist/index.html
! grep -qwi 'amazing' dist/index.html
! grep -qwi 'blazing' dist/index.html
! grep -qwi 'effortless' dist/index.html
! grep -qwi 'powerful' dist/index.html
! grep -qF 'millisecond inference' dist/index.html
! grep -qF 'CoreML on Apple Neural Engine' dist/index.html
# Positive control: new string in rendered HTML
grep -qF 'Add the MCP snippet' dist/index.html
echo "04-02 VERIFY: PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/components/HowItWorks.astro` line 4 contains exactly: `Add the MCP snippet. Paste the behavioural prompt. Done.`
    - `src/components/HowItWorks.astro` does NOT contain the string `Connect your AI client to Covalence's MCP server`
    - `src/components/HowItWorks.astro` still contains `Two copy-pastes. That's it.` (heading unchanged)
    - `src/components/HowItWorks.astro` still contains `cov-mcp` (code block unchanged)
    - `npm run build` exits 0
    - `dist/index.html` contains `Add the MCP snippet`
    - `dist/index.html` has zero occurrences of these banned words (whole-word, case-insensitive): instant, amazing, blazing, effortless, powerful
    - `dist/index.html` does not contain the strings `millisecond inference` or `CoreML on Apple Neural Engine`
  </acceptance_criteria>
  <done>HowItWorks.astro subtitle reads "Add the MCP snippet. Paste the behavioural prompt. Done." — tightened for power users, build passes, voice invariants clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. Static string replacement in a build-time template.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering | HowItWorks.astro | accept | Single-line prose edit in a static template; no injection surface. Build gate catches malformed HTML. |
</threat_model>

<verification>
- `npm run build` exits 0
- `dist/index.html` contains the new subtitle string
- `dist/index.html` does not contain the old subtitle
- Voice-invariant grep matrix passes (D-13)
</verification>

<success_criteria>
Plan 04-02 is complete when:
- HowItWorks.astro line 4 reads the D-04 target string verbatim
- Build passes
- No banned voice words in dist/index.html
- No collateral changes to any other line in HowItWorks.astro
</success_criteria>

<output>
After completion, create `.planning/phases/04-marketing-copy-rewrite-pro-tier-readiness/04-02-SUMMARY.md`
</output>
