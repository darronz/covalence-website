---
phase: 04-marketing-copy-rewrite-pro-tier-readiness
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [src/components/Footer.astro]
autonomous: true
requirements: [COPY-04]

must_haves:
  truths:
    - "Footer tagline reads 'Built for the machines you own' — no Mac-specific language"
    - "No banned voice words appear in the rendered Footer section of dist/index.html"
    - "The download CTA, footer links row (Releases, Latest Release, RSS, Contact, Report an issue), copyright line, and all CSS are byte-identical to pre-edit"
  artifacts:
    - path: "src/components/Footer.astro"
      provides: "Pro-tier-ready footer tagline"
      contains: "Built for the machines you own"
  key_links:
    - from: "src/components/Footer.astro"
      to: "dist/index.html"
      via: "Astro SSG build"
      pattern: "Built for the machines you own"
---

<objective>
Replace the Footer.astro tagline (line 8) with the locked target string from D-11, removing the Mac-specific "Built on macOS, for macOS" claim.

Purpose: COPY-04 requires the footer tagline to read correctly for any host shell the product ships (not Mac-only). The target string "Built for the machines you own" is tier-neutral and echoes the load-bearing "machines you own" framing from the hero subhead.

Output: Modified `src/components/Footer.astro` with the new tagline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-marketing-copy-rewrite-pro-tier-readiness/04-CONTEXT.md

<interfaces>
<!-- Current Footer.astro line 8 (the ONLY line this plan touches): -->
```html
    <h2>Built on macOS, for macOS</h2>
```

<!-- Target replacement (D-11, verbatim): -->
```html
    <h2>Built for the machines you own</h2>
```

<!-- Lines that MUST NOT change: -->
Lines 1-4: Frontmatter (import, hasPosts constant)
Line 6: `<section id="download" class="section download-section">`
Line 9: `<p>Give your AI the memory it's missing.</p>`
Lines 10-17: Download CTA (anchor + SVG icon + "Download for macOS" text)
Lines 20-31: Footer links row (Releases, Latest Release, RSS conditional, Contact, Report an issue) + copyright
Lines 33-108: All CSS unchanged

NOTE on "Download for macOS" (line 15): This text is the CTA button label, NOT the tagline. D-11 / COPY-04 applies only to the `<h2>` tagline. The download button text stays "Download for macOS" because the product is still macOS-only today — this is consistent with CONTEXT.md's out-of-scope note: "Do NOT remove the v1.3.5 download-required banner, nav, download button, version string, or release notes link."

D-13 `free` scoping note: The word "free" does not appear in Footer.astro currently or in the target string. No special scoping needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Footer.astro tagline with D-11 target string</name>
  <files>src/components/Footer.astro</files>
  <read_first>
    - src/components/Footer.astro (current state — confirm line 8 reads "Built on macOS, for macOS")
    - .planning/phases/04-marketing-copy-rewrite-pro-tier-readiness/04-CONTEXT.md (D-11 locked target string)
  </read_first>
  <action>
Edit `src/components/Footer.astro` line 8. Replace:

```html
    <h2>Built on macOS, for macOS</h2>
```

With:

```html
    <h2>Built for the machines you own</h2>
```

This is a single-line replacement inside `<section id="download">`. Do NOT touch any other line in the file. The download CTA "Download for macOS" (line 15), the sub-copy "Give your AI the memory it's missing." (line 9), the footer links (lines 20-31), and all CSS (lines 33-108) remain byte-identical.

Per D-11: "Built on macOS, for macOS" is Mac-specific and won't survive the Pro-tier split. "Built for the machines you own" echoes the hero subhead's "machines you own" framing.
  </action>
  <verify>
    <automated>
set -e
# Positive: new string present
grep -qF 'Built for the machines you own' src/components/Footer.astro
# Negative: old string gone
! grep -qF 'Built on macOS, for macOS' src/components/Footer.astro
# Collateral check: download CTA unchanged
grep -qF 'Download for macOS' src/components/Footer.astro
# Collateral check: sub-copy unchanged
grep -qF "Give your AI the memory it's missing." src/components/Footer.astro
# Collateral check: footer links unchanged
grep -qF 'href="/releases/"' src/components/Footer.astro
grep -qF 'href="mailto:hello@covalence.app"' src/components/Footer.astro
# Build check
npm run build
# Voice invariants on dist/index.html (D-13)
! grep -qwi 'instant' dist/index.html
! grep -qwi 'amazing' dist/index.html
! grep -qwi 'blazing' dist/index.html
! grep -qwi 'effortless' dist/index.html
! grep -qwi 'powerful' dist/index.html
! grep -qF 'millisecond inference' dist/index.html
! grep -qF 'CoreML on Apple Neural Engine' dist/index.html
# Positive control: new string in rendered HTML
grep -qF 'Built for the machines you own' dist/index.html
# Negative control: old string gone from rendered HTML
! grep -qF 'Built on macOS, for macOS' dist/index.html
echo "04-05 VERIFY: PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Footer.astro` line 8 contains exactly: `<h2>Built for the machines you own</h2>`
    - `src/components/Footer.astro` does NOT contain the string `Built on macOS, for macOS`
    - `src/components/Footer.astro` still contains `Download for macOS` (CTA button unchanged)
    - `src/components/Footer.astro` still contains `Give your AI the memory it's missing.` (sub-copy unchanged)
    - `src/components/Footer.astro` still contains `href="/releases/"` (footer links unchanged)
    - `npm run build` exits 0
    - `dist/index.html` contains `Built for the machines you own`
    - `dist/index.html` does NOT contain `Built on macOS, for macOS`
    - `dist/index.html` has zero occurrences of these banned words (whole-word, case-insensitive): instant, amazing, blazing, effortless, powerful
    - `dist/index.html` does not contain the strings `millisecond inference` or `CoreML on Apple Neural Engine`
  </acceptance_criteria>
  <done>Footer.astro tagline reads "Built for the machines you own" — no Mac-specific language, build passes, voice invariants clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. Static string replacement in a build-time template.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | Footer.astro | accept | Single-line prose edit in a static template; no injection surface. Build gate catches malformed HTML. |
</threat_model>

<verification>
- `npm run build` exits 0
- `dist/index.html` contains the new tagline
- `dist/index.html` does not contain the old tagline
- Voice-invariant grep matrix passes (D-13)
</verification>

<success_criteria>
Plan 04-05 is complete when:
- Footer.astro line 8 reads the D-11 target string verbatim
- Build passes
- No banned voice words in dist/index.html
- No collateral changes to any other line in Footer.astro
</success_criteria>

<output>
After completion, create `.planning/phases/04-marketing-copy-rewrite-pro-tier-readiness/04-05-SUMMARY.md`
</output>
