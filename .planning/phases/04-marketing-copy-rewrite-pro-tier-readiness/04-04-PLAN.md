---
phase: 04-marketing-copy-rewrite-pro-tier-readiness
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [src/components/Architecture.astro]
autonomous: true
requirements: [COPY-02]

must_haves:
  truths:
    - "Architecture arch-stack teaser reads 'Every memory becomes a 256-dimension vector on hardware you control. Vector similarity meets keyword match via RRF. No third-party cloud, by architecture.' (4th repetition of load-bearing principle)"
    - "Architecture arch-privacy heading reads 'Your data, your infrastructure' and body matches D-10 target verbatim"
    - "This plan INTENTIONALLY breaks Phase 3's byte-identical arch-privacy invariant — flag in commit message"
    - "No banned voice words appear in Architecture section of dist/index.html"
    - "The arch-teaser-detail paragraph, arch-teaser-cta paragraph with /docs/under-the-hood/ link, and all CSS are unchanged"
  artifacts:
    - path: "src/components/Architecture.astro"
      provides: "Pro-tier-ready architecture copy — arch-stack teaser + arch-privacy rewrite"
      contains: "No third-party cloud, by architecture"
  key_links:
    - from: "src/components/Architecture.astro"
      to: "dist/index.html"
      via: "Astro SSG build"
      pattern: "No third-party cloud, by architecture"
---

<objective>
Rewrite two sections of Architecture.astro: (1) the arch-stack teaser paragraph (line 8) per D-09, and (2) the arch-privacy column heading + body (lines 20-28) per D-10. This makes Architecture.astro carry two of the four "no third-party cloud" repetitions.

Purpose: COPY-02 requires the privacy-architecture paragraph to be reframed as "Your data, your infrastructure" leading with "no third-party cloud, by architecture". D-09 adds a fourth repetition of the load-bearing principle to the teaser.

**IMPORTANT: This plan intentionally breaks Phase 3 Plan 03-03's byte-identical arch-privacy invariant.** Phase 3 preserved the privacy column untouched. Phase 4 rewrites it per D-10. This is NOT a regression — it is the intended Phase 4 scope. Flag in the commit message.

Output: Modified `src/components/Architecture.astro` with both rewrites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-marketing-copy-rewrite-pro-tier-readiness/04-CONTEXT.md
@.planning/phases/03-content-depth-seo/03-03-SUMMARY.md

<interfaces>
<!-- Current Architecture.astro — lines being edited -->

<!-- Line 8 (arch-stack teaser .arch-teaser) — CURRENT: -->
```
          Every memory becomes a 256-dimension vector. Search runs on-device. Vector similarity meets keyword match via RRF. No cloud round-trips.
```
<!-- Line 8 — TARGET (D-09): -->
```
          Every memory becomes a 256-dimension vector on hardware you control. Vector similarity meets keyword match via RRF. No third-party cloud, by architecture.
```

<!-- Lines 20-28 (arch-privacy column) — CURRENT: -->
```html
      <div class="arch-privacy">
        <h3>Privacy by architecture</h3>
        <p class="privacy-statement">
          Your data never leaves your machine. No accounts. No telemetry. No cloud. Runs entirely on-device.
        </p>
        <p class="privacy-detail">
          Every component — the database, the embedding model, the search engine — runs locally.
          There are no network calls, no API keys, no external dependencies.
          Your memories are a single SQLite file on your Mac that you own, back up, and export whenever you want.
        </p>
      </div>
```
<!-- Lines 20-28 — TARGET (D-10): -->
```html
      <div class="arch-privacy">
        <h3>Your data, your infrastructure</h3>
        <p class="privacy-statement">
          Covalence runs on machines you control. No accounts with us. No telemetry. No third-party cloud, by architecture.
        </p>
        <p class="privacy-detail">
          Every component — the database, the embedding model, the search engine — runs on your hardware.
          No network calls to us, no API keys to buy, no external dependencies to trust.
          Your memories are a SQLite file you own, back up, and export whenever you want.
        </p>
      </div>
```

<!-- Lines that MUST NOT change: -->
Line 3: `<h2 class="section-title">Under the hood</h2>`
Line 6: `<h3>How retrieval works</h3>`
Lines 10-13: arch-teaser-detail paragraph (nomic-embed-text, RRF at k=60, 10% recency — Phase 3 content)
Lines 15-17: arch-teaser-cta paragraph with /docs/under-the-hood/ link
Lines 34-98: All CSS rules unchanged (selectors .arch-privacy h3, .privacy-statement, .privacy-detail all still apply since classes are preserved)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite arch-stack teaser (D-09) + arch-privacy column (D-10)</name>
  <files>src/components/Architecture.astro</files>
  <read_first>
    - src/components/Architecture.astro (current state — verify lines 8, 20-28)
    - .planning/phases/04-marketing-copy-rewrite-pro-tier-readiness/04-CONTEXT.md (D-09, D-10 locked target strings)
    - .planning/phases/03-content-depth-seo/03-03-SUMMARY.md (understand the Phase 3 byte-identical invariant being intentionally broken)
  </read_first>
  <action>
Edit `src/components/Architecture.astro` in two places:

**Edit 1 — arch-stack teaser (line 8, inside `<p class="arch-teaser">`):**

Replace:
```
          Every memory becomes a 256-dimension vector. Search runs on-device. Vector similarity meets keyword match via RRF. No cloud round-trips.
```

With:
```
          Every memory becomes a 256-dimension vector on hardware you control. Vector similarity meets keyword match via RRF. No third-party cloud, by architecture.
```

Changes: "Search runs on-device." removed. "on hardware you control" appended to the first sentence. "No cloud round-trips." replaced with "No third-party cloud, by architecture." — the 4th repetition of the load-bearing principle (joining hero subhead, features tile #5, and arch-privacy below).

**Edit 2 — arch-privacy column (lines 20-28, the `<div class="arch-privacy">` block):**

Replace the heading:
```html
        <h3>Privacy by architecture</h3>
```
With:
```html
        <h3>Your data, your infrastructure</h3>
```

Replace the privacy-statement paragraph content:
```
          Your data never leaves your machine. No accounts. No telemetry. No cloud. Runs entirely on-device.
```
With:
```
          Covalence runs on machines you control. No accounts with us. No telemetry. No third-party cloud, by architecture.
```

Replace the privacy-detail paragraph content:
```
          Every component — the database, the embedding model, the search engine — runs locally.
          There are no network calls, no API keys, no external dependencies.
          Your memories are a single SQLite file on your Mac that you own, back up, and export whenever you want.
```
With:
```
          Every component — the database, the embedding model, the search engine — runs on your hardware.
          No network calls to us, no API keys to buy, no external dependencies to trust.
          Your memories are a SQLite file you own, back up, and export whenever you want.
```

**DO NOT TOUCH:**
- Line 3: section title "Under the hood"
- Line 6: arch-stack heading "How retrieval works"
- Lines 10-13: arch-teaser-detail paragraph (Phase 3 content with nomic-embed-text-v1.5, RRF at k=60, 10% recency)
- Lines 15-17: arch-teaser-cta with `/docs/under-the-hood/` link
- Lines 34-98: All CSS rules (the `.arch-privacy h3`, `.privacy-statement`, `.privacy-detail` selectors all still apply since the HTML classes are preserved)

**Commit message MUST include:** "intentionally breaks Phase 3 Plan 03-03's byte-identical arch-privacy invariant per D-10 / COPY-02"
  </action>
  <verify>
    <automated>
set -e
# Edit 1 — arch-stack teaser
grep -qF 'Every memory becomes a 256-dimension vector on hardware you control.' src/components/Architecture.astro
grep -qF 'No third-party cloud, by architecture.' src/components/Architecture.astro
! grep -qF 'Search runs on-device.' src/components/Architecture.astro
! grep -qF 'No cloud round-trips.' src/components/Architecture.astro
# Edit 2 — arch-privacy heading
grep -qF 'Your data, your infrastructure' src/components/Architecture.astro
! grep -qF 'Privacy by architecture' src/components/Architecture.astro
# Edit 2 — privacy-statement
grep -qF 'Covalence runs on machines you control.' src/components/Architecture.astro
grep -qF 'No accounts with us.' src/components/Architecture.astro
! grep -qF 'Your data never leaves your machine.' src/components/Architecture.astro
! grep -qF 'Runs entirely on-device.' src/components/Architecture.astro
# Edit 2 — privacy-detail
grep -qF 'runs on your hardware.' src/components/Architecture.astro
grep -qF 'No network calls to us, no API keys to buy' src/components/Architecture.astro
grep -qF 'a SQLite file you own' src/components/Architecture.astro
! grep -qF 'runs locally.' src/components/Architecture.astro
! grep -qF 'a single SQLite file on your Mac' src/components/Architecture.astro
# Unchanged content
grep -qF 'Under the hood' src/components/Architecture.astro
grep -qF 'How retrieval works' src/components/Architecture.astro
grep -qF 'nomic-embed-text-v1.5' src/components/Architecture.astro
grep -qF 'Reciprocal Rank Fusion at k=60' src/components/Architecture.astro
grep -qF 'href="/docs/under-the-hood/"' src/components/Architecture.astro
# CSS preserved
grep -qF '.privacy-statement' src/components/Architecture.astro
grep -qF '.privacy-detail' src/components/Architecture.astro
grep -qF '.arch-teaser' src/components/Architecture.astro
# Build check
npm run build
# Voice invariants on dist/index.html (D-13)
! grep -qwi 'instant' dist/index.html
! grep -qwi 'amazing' dist/index.html
! grep -qwi 'blazing' dist/index.html
! grep -qwi 'effortless' dist/index.html
! grep -qwi 'powerful' dist/index.html
! grep -qF 'millisecond inference' dist/index.html
! grep -qF 'CoreML on Apple Neural Engine' dist/index.html
# Positive controls in rendered HTML (D-15 norm: use grep -oE | wc -l for counts)
grep -oE 'No third-party cloud, by architecture' dist/index.html | wc -l | tr -d ' ' | grep -qx '2'
grep -qF 'Your data, your infrastructure' dist/index.html
grep -qF 'Covalence runs on machines you control' dist/index.html
echo "04-04 VERIFY: PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Architecture.astro` contains `Every memory becomes a 256-dimension vector on hardware you control.`
    - `src/components/Architecture.astro` contains the string `No third-party cloud, by architecture.` at least twice (once in arch-teaser, once in privacy-statement)
    - `src/components/Architecture.astro` heading reads `Your data, your infrastructure` (not `Privacy by architecture`)
    - `src/components/Architecture.astro` contains `Covalence runs on machines you control.`
    - `src/components/Architecture.astro` contains `runs on your hardware.` (not `runs locally.`)
    - `src/components/Architecture.astro` contains `a SQLite file you own` (not `a single SQLite file on your Mac`)
    - `src/components/Architecture.astro` does NOT contain `Privacy by architecture`, `Your data never leaves your machine`, `Runs entirely on-device`, `runs locally.`, `a single SQLite file on your Mac`, `Search runs on-device.`, `No cloud round-trips.`
    - `src/components/Architecture.astro` STILL contains `nomic-embed-text-v1.5`, `Reciprocal Rank Fusion at k=60`, `href="/docs/under-the-hood/"` (Phase 3 content preserved)
    - `src/components/Architecture.astro` STILL contains CSS selectors `.privacy-statement`, `.privacy-detail`, `.arch-teaser` (classes unchanged)
    - `npm run build` exits 0
    - `dist/index.html` contains `No third-party cloud, by architecture` exactly 2 times (verified with `grep -oE | wc -l`)
    - `dist/index.html` has zero occurrences of banned words: instant, amazing, blazing, effortless, powerful, millisecond inference, CoreML on Apple Neural Engine
  </acceptance_criteria>
  <done>Architecture.astro carries two repetitions of "No third-party cloud, by architecture" — arch-stack teaser and arch-privacy both rewritten per D-09/D-10. Phase 3 byte-identical invariant intentionally broken. Build passes, voice invariants clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. Static string replacements in a build-time template.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | Architecture.astro | accept | Prose edits in a static template; no injection surface. Build gate catches malformed HTML. |
</threat_model>

<verification>
- `npm run build` exits 0
- `dist/index.html` contains both new paragraph strings
- `dist/index.html` does not contain old paragraph strings
- "No third-party cloud, by architecture" appears exactly 2 times in dist/index.html
- Voice-invariant grep matrix passes (D-13)
- Phase 3 content (teaser-detail, teaser-cta) preserved
</verification>

<success_criteria>
Plan 04-04 is complete when:
- Arch-stack teaser reads D-09 target verbatim
- Arch-privacy heading + body read D-10 target verbatim
- Phase 3 content (teaser-detail, CTA) unchanged
- CSS selectors unchanged
- Build passes
- "No third-party cloud, by architecture" count = 2 in dist/index.html
- No banned voice words in dist/index.html
</success_criteria>

<output>
After completion, create `.planning/phases/04-marketing-copy-rewrite-pro-tier-readiness/04-04-SUMMARY.md`
</output>
