---
phase: 04-marketing-copy-rewrite-pro-tier-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/Hero.astro]
autonomous: true
requirements: [COPY-01]

must_haves:
  truths:
    - "Hero subhead reads 'Your memory, on machines you own. Never anyone else''s cloud.' — no Mac-specific language"
    - "No banned voice words appear in the rendered Hero section of dist/index.html"
    - "The hero headline, CTA, version meta, and all other Hero.astro content outside line 23 are byte-identical to pre-edit"
  artifacts:
    - path: "src/components/Hero.astro"
      provides: "Pro-tier-ready hero subhead"
      contains: "Your memory, on machines you own. Never anyone else's cloud."
  key_links:
    - from: "src/components/Hero.astro"
      to: "dist/index.html"
      via: "Astro SSG build"
      pattern: "Your memory, on machines you own"
---

<objective>
Replace the Hero.astro subhead (line 23) with the locked target string from D-03, removing the Mac-specific "Runs on your Mac" claim.

Purpose: COPY-01 requires the hero subhead to commit to "machines you own" / "never anyone else's cloud" rather than a Mac-specific promise. This is the most visible single-line edit in the phase — the first sentence a visitor reads after the headline.

Output: Modified `src/components/Hero.astro` with the new subhead.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-marketing-copy-rewrite-pro-tier-readiness/04-CONTEXT.md

<interfaces>
<!-- Current Hero.astro line 23 (the ONLY line this plan touches): -->
```
      Runs on your Mac. Private, fast, and entirely yours.
```

<!-- Target replacement (D-03, verbatim): -->
```
      Your memory, on machines you own. Never anyone else's cloud.
```

<!-- Lines that MUST NOT change: -->
Line 21: `<h1 class="hero-headline">Every AI you use, one shared memory</h1>`
Line 25-33: Download CTA block (href, SVG icon, "Download for macOS" text)
Line 34-36: Version meta (`Version {latestRelease.version} ...`)
Lines 40-117: All CSS unchanged
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Hero.astro subhead with D-03 target string</name>
  <files>src/components/Hero.astro</files>
  <read_first>
    - src/components/Hero.astro (current state — confirm line 23 still reads "Runs on your Mac. Private, fast, and entirely yours.")
    - .planning/phases/04-marketing-copy-rewrite-pro-tier-readiness/04-CONTEXT.md (D-03 locked target string)
  </read_first>
  <action>
Edit `src/components/Hero.astro` line 23. Replace:

```
      Runs on your Mac. Private, fast, and entirely yours.
```

With:

```
      Your memory, on machines you own. Never anyone else's cloud.
```

This is a single-line replacement inside the `<p class="hero-subhead">` element. Do NOT touch any other line in the file. The headline (line 21), the download CTA (lines 25-33), the version meta (lines 34-36), and all CSS (lines 40-117) remain byte-identical.

Per D-03: this removes the Mac-specific "Runs on your Mac" and replaces it with the tier-neutral "machines you own" framing. The second sentence "Never anyone else's cloud" is the hero-register instance of the load-bearing principle (1 of 4 repetitions across the page).
  </action>
  <verify>
    <automated>
set -e
# Positive: new string present on line 23
grep -qF "Your memory, on machines you own. Never anyone else's cloud." src/components/Hero.astro
# Negative: old string gone
! grep -qF 'Runs on your Mac' src/components/Hero.astro
# Collateral check: headline unchanged
grep -qF 'Every AI you use, one shared memory' src/components/Hero.astro
# Collateral check: download CTA unchanged
grep -qF 'Download for macOS' src/components/Hero.astro
# Build check
npm run build
# Voice invariants on dist/index.html (D-13 banned words)
! grep -qwi 'instant' dist/index.html
! grep -qwi 'amazing' dist/index.html
! grep -qwi 'blazing' dist/index.html
! grep -qwi 'effortless' dist/index.html
! grep -qwi 'powerful' dist/index.html
! grep -qF 'millisecond inference' dist/index.html
! grep -qF 'CoreML on Apple Neural Engine' dist/index.html
# Positive control: new string in rendered HTML
grep -qF 'Your memory, on machines you own' dist/index.html
echo "04-01 VERIFY: PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Hero.astro` line 23 contains exactly: `Your memory, on machines you own. Never anyone else's cloud.`
    - `src/components/Hero.astro` does NOT contain the string `Runs on your Mac`
    - `src/components/Hero.astro` still contains `Every AI you use, one shared memory` (headline unchanged)
    - `src/components/Hero.astro` still contains `Download for macOS` (CTA unchanged)
    - `npm run build` exits 0
    - `dist/index.html` contains `Your memory, on machines you own`
    - `dist/index.html` has zero occurrences of these banned words (whole-word, case-insensitive): instant, amazing, blazing, effortless, powerful
    - `dist/index.html` does not contain the strings `millisecond inference` or `CoreML on Apple Neural Engine`
  </acceptance_criteria>
  <done>Hero.astro subhead reads "Your memory, on machines you own. Never anyone else's cloud." — no Mac-specific language, build passes, voice invariants clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. This is a static string replacement in a build-time template. No user input, no runtime behaviour change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Hero.astro | accept | Single-line prose edit in a static template; no injection surface. Build gate catches malformed HTML. |
</threat_model>

<verification>
- `npm run build` exits 0
- `dist/index.html` contains the new subhead string
- `dist/index.html` does not contain "Runs on your Mac"
- Voice-invariant grep matrix passes (D-13)
</verification>

<success_criteria>
Plan 04-01 is complete when:
- Hero.astro line 23 reads the D-03 target string verbatim
- Build passes
- No banned voice words in dist/index.html
- No collateral changes to any other line in Hero.astro
</success_criteria>

<output>
After completion, create `.planning/phases/04-marketing-copy-rewrite-pro-tier-readiness/04-01-SUMMARY.md`
</output>
