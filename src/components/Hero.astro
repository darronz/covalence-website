<section class="hero">
  <div class="container hero-inner">
    <div class="hero-icon">
      <img src="/icon.png" alt="Covalence" width="80" height="80" />
    </div>
    <h1 class="hero-headline">Every AI conversation starts from scratch</h1>
    <p class="hero-subhead">
      Covalence gives your AI persistent memory — locally, privately, across every client.
    </p>
    <div class="hero-cta">
      <a href="https://covalence.app/releases/Covalence-latest.dmg" class="btn btn-primary">
        <svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
          <path d="M8 12L3 7l1.4-1.4L7 8.2V1h2v7.2l2.6-2.6L13 7l-5 5z" fill="currentColor"/>
          <path d="M2 14v-3h2v1h8v-1h2v3H2z" fill="currentColor"/>
        </svg>
        Download for macOS
      </a>
    </div>
    <p class="hero-meta">
      Version 1.2.1 · macOS 15+ · ~104 MB
    </p>
  </div>
</section>

<style>
  .hero {
    padding-top: calc(var(--nav-height) + var(--space-2xl));
    padding-bottom: var(--space-2xl);
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  /* Atmospheric glow — warm light emerging from darkness */
  .hero::before {
    content: '';
    position: absolute;
    top: 20%;
    left: 50%;
    transform: translateX(-50%);
    width: 600px;
    height: 600px;
    background: radial-gradient(
      ellipse at center,
      rgba(232, 152, 58, 0.08) 0%,
      rgba(167, 139, 250, 0.04) 40%,
      transparent 70%
    );
    pointer-events: none;
    z-index: 0;
  }

  .hero-inner {
    max-width: 720px;
    position: relative;
    z-index: 1;
  }

  .hero-icon {
    margin-bottom: var(--space-lg);
  }

  .hero-icon img {
    margin: 0 auto;
    border-radius: 16px;
    box-shadow:
      0 0 60px rgba(232, 152, 58, 0.2),
      0 0 120px rgba(232, 152, 58, 0.08),
      0 0 200px rgba(167, 139, 250, 0.05);
  }

  .hero-headline {
    font-size: 3.25rem;
    font-weight: 700;
    line-height: 1.1;
    letter-spacing: -0.02em;
    margin-bottom: var(--space-lg);
  }

  .hero-subhead {
    font-size: 1.25rem;
    color: var(--text-secondary);
    line-height: 1.5;
    margin-bottom: var(--space-xl);
  }

  .hero-cta {
    margin-bottom: var(--space-md);
  }

  .hero-meta {
    font-size: 0.8125rem;
    color: var(--text-secondary);
  }

  @media (max-width: 768px) {
    .hero-headline {
      font-size: 2.25rem;
    }

    .hero-subhead {
      font-size: 1.125rem;
    }
  }
</style>
